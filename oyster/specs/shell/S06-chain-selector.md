---
task_id: S06-chain-selector
project: shell-vibe-ide
priority: 2
estimated_minutes: 20
depends_on: ["S01-fork-bolt-diy", "S02-cyberpunk-theme"]
modifies: ["web-ui/app/components/chain-selector.tsx", "web-ui/app/stores/chain-store.ts", "web-ui/app/components/header.tsx"]
executor: glm
---

## 目标

在 IDE 顶栏加入 SVM / EVM 链选择器，决定 AI 生成的语言和构建工具链。

## UI 设计

```
┌──────────────────────────────────────────┐
│  ◉ Shell    [SVM ▾] [Devnet ▾]   [🔗]   │
└──────────────────────────────────────────┘
```

### 链选择器 (左)
- 两个选项: `SVM` / `EVM`
- 样式: 胶囊按钮, 选中的发光 (#00ff88)
- 切换链时:
  - 更新 prompt 模板列表 (S04)
  - 更新构建命令 (S05)
  - 更新网络选择器选项

### 网络选择器 (右)
- SVM 模式:
  - Devnet (默认)
  - Testnet
  - Mainnet-beta
- EVM 模式:
  - Anvil (本地, 默认)
  - Sepolia
  - Base Sepolia
  - Mainnet

### 状态存储
- 使用 React context 或 zustand store
- `chainType: 'svm' | 'evm'`
- `network: string`
- 其他组件从 context 读取链状态

## 约束

- 只做 UI + 状态管理
- 不要实际连接链 (Sprint 2 做)
- 赛博朋克风格

## 验收标准

- [ ] 顶栏显示链选择器
- [ ] 可以切换 SVM / EVM
- [ ] 网络列表根据链动态变化
- [ ] 链状态可被其他组件读取
- [ ] 符合赛博朋克主题

## 不要做

- 不要实现钱包连接
- 不要实现链上查询
- 不要碰 desktop/ 和 runner/
- 不要写 TODO/FIXME 注释，所有功能必须完整实现
