---
task_id: G2-01-AI
project: ai-powered-nft-valuation
priority: 1
depends_on: []
modifies: ["nft_valuation.py", "tests/test_nft_valuation.py"]
executor: glm
---
task_id: G2-01-AI  
project: ai-powered-nft-valuation  
priority: 1  
depends_on: []  
modifies: ["nft_valuation.py", "tests/test_nft_valuation.py"]  
---  
## 目标  
实现 NFT 基础价值评估功能。  

## 技术方案  
1. 在 `nft_valuation.py` 中添加 `calculate_nft_value` 函数，使用简单的线性回归模型根据 NFT 的特征（如稀有度、历史交易价格）计算其价值。  
2. 在 `tests/test_nft_valuation.py` 中编写测试用例，验证函数在不同输入下的输出是否符合预期。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保函数在不同场景下的正确性。  

## 验收标准  
- [ ] `calculate_nft_value` 函数能够根据输入特征返回合理的 NFT 价值评估。  
- [ ] pytest 全绿，所有测试用例通过。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。