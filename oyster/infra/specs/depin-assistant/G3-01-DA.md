---
task_id: G3-01-DA
project: depin-assistant
priority: 1
depends_on: []
modifies: ["src/nodeManager.js", "tests/nodeManager.test.js"]
executor: glm
---
task_id: G3-01-DA
project: depin-assistant
priority: 1
depends_on: []
modifies: ["src/nodeManager.js", "tests/nodeManager.test.js"]
---
## 目标
实现基本的DePIN节点状态监控功能。

## 技术方案
1. 在 `nodeManager.js` 中添加一个函数 `getNodeStatus`，使用 Ethers.js 与节点进行交互以获取状态。
2. 使用 Node.js 的定时器每5分钟调用一次 `getNodeStatus` 并记录状态。
3. 在 `nodeManager.test.js` 中编写测试，模拟节点状态返回并验证函数返回值。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试以验证状态监控功能。

## 验收标准
- [ ] `getNodeStatus` 函数能够正确获取节点状态。
- [ ] 定时器每5分钟正确调用 `getNodeStatus`。
- [ ] 状态记录功能正常。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。