---
task_id: G5-04-DA
project: depin-assistant
priority: 3
depends_on: []
modifies: ["src/user_interface.py", "tests/test_user_interface.py"]
executor: glm
---
task_id: G5-04-DA
project: depin-assistant
priority: 3
depends_on: []
modifies: ["src/user_interface.py", "tests/test_user_interface.py"]
---
## 目标
增强用户界面功能，提供更直观的节点管理体验。

## 技术方案
1. 在 `user_interface.py` 中添加一个仪表板，显示节点状态和性能指标。
2. 使用 Python 的图形库（如 Matplotlib）生成实时图表，展示节点运行数据。
3. 允许用户通过界面手动重启节点或调整配置。
4. 在 `tests/test_user_interface.py` 中编写单元测试，确保界面功能正常。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 编写基础测试，确保界面功能正常

## 验收标准
- [ ] 仪表板显示节点状态和性能指标
- [ ] 实时图表能够正确反映节点运行数据
- [ ] 用户能够通过界面手动操作节点
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS