---
task_id: G8-03-OI
project: oyster-infra
priority: 1
depends_on: []
modifies: ["deployer.py", "deployment_config.yaml"]
executor: glm
---
task_id: G8-03-OI  
project: oyster-infra  
priority: 1  
depends_on: []  
modifies: ["deployer.py", "deployment_config.yaml"]  

---
## 目标
实现自动化蓝绿部署功能。

## 技术方案
1. 在 `deployer.py` 中添加蓝绿部署逻辑，支持新旧版本并行运行。
2. 更新 `deployment_config.yaml` 以包含蓝绿部署的配置选项。
3. 实现版本切换机制，根据健康检查结果自动切换流量到新版本。
4. 添加回滚功能，在检测到新版本故障时自动回滚到旧版本。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保蓝绿部署功能在模拟环境下正常运行。

## 验收标准
- [ ] 蓝绿部署功能在模拟部署中，切换时间不超过 5 秒。
- [ ] pytest 全绿。

## 不要做
- 不重构现有部署逻辑。
- 不改 UI/CSS。