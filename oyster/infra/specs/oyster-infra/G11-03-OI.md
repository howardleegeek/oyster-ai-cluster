---
task_id: G11-03-OI
project: oyster-infra
priority: 1
depends_on: []
modifies: ["authentication.py", "database.py"]
executor: glm
---
task_id: G11-03-OI  
project: oyster-infra  
priority: 1  
depends_on: []  
modifies: ["authentication.py", "database.py"]  

## 目标  
加强系统安全性，引入多因素认证（MFA）机制。  

## 技术方案  
- 在 `authentication.py` 中集成 MFA 支持，使用 TOTP（基于时间的一次性密码）算法。  
- 修改 `database.py` 以存储 MFA 相关信息，如密钥和恢复码。  
- 更新用户登录流程以支持 MFA 验证。  
- 编写测试用例以确保 MFA 功能的正确性。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试以验证 MFA 机制的安全性。  

## 验收标准  
- [ ] 用户在登录时需要通过 MFA 验证才能访问系统。  
- [ ] 现有功能测试通过，pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。