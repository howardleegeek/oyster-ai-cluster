---
task_id: G10-01-OI
project: oyster-infra
priority: 1
depends_on: []
modifies: ["scheduler.py", "tests/test_scheduler.py"]
executor: glm
---
task_id: G10-01-OI  
project: oyster-infra  
priority: 1  
depends_on: []  
modifies: ["scheduler.py", "tests/test_scheduler.py"]  
---  
## 目标  
在调度系统中实现任务优先级动态调整功能，以提高关键任务的执行效率。  

## 技术方案  
- 在 `scheduler.py` 中添加一个函数，用于根据任务优先级动态调整调度队列。
- 使用现有的任务元数据来计算优先级权重。
- 修改调度循环以优先处理高优先级任务。
- 在 `tests/test_scheduler.py` 中添加单元测试，确保优先级调整功能按预期工作。

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保功能正确性。  

## 验收标准  
- [ ] 调度系统能够根据任务优先级动态调整执行顺序。  
- [ ] 高优先级任务在调度队列中优先被处理。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。