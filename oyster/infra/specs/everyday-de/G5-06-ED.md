---
task_id: G5-06-ED
project: everyday-de
priority: 3
depends_on: []
modifies: ["src/wallet/security_module.py", "tests/test_security_module.py"]
executor: glm
---
task_id: G5-06-ED
project: everyday-de
priority: 3
depends_on: []
modifies: ["src/wallet/security_module.py", "tests/test_security_module.py"]
---
## 目标
增强钱包的安全性，添加多因素认证和交易限额功能。

## 技术方案
1. 在 `security_module.py` 中实现 `SecurityModule` 类，添加方法 `enable_mfa`, `disable_mfa`, 和 `set_transaction_limit`。
2. 使用第三方库实现多因素认证，例如 Google Authenticator。
3. 在 `tests/test_security_module.py` 中编写单元测试，覆盖主要功能路径。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 编写单元测试，确保主要功能路径被覆盖

## 验收标准
- [ ] 用户可以启用和禁用多因素认证
- [ ] 用户可以设置交易限额
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS