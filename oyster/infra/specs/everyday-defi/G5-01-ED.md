---
task_id: G5-01-ED
project: everyday-defi
priority: 1
depends_on: []
modifies: ["src/wallet/transaction_manager.py", "tests/test_transaction_manager.py"]
executor: glm
---
task_id: G5-01-ED
project: everyday-defi
priority: 1
depends_on: []
modifies: ["src/wallet/transaction_manager.py", "tests/test_transaction_manager.py"]
---
## 目标
实现基本的交易管理功能，支持用户发起、签名和广播交易。

## 技术方案
1. 在 `transaction_manager.py` 中创建 `TransactionManager` 类，包含方法 `initiate_transaction`, `sign_transaction`, 和 `broadcast_transaction`。
2. 使用 Node.js 后端提供的 API 进行交易签名和广播。
3. 在 `tests/test_transaction_manager.py` 中编写单元测试，覆盖主要功能路径。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 编写基础测试，确保主要功能路径被覆盖

## 验收标准
- [ ] 用户可以发起交易
- [ ] 交易可以被签名
- [ ] 交易可以被广播到区块链网络
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS