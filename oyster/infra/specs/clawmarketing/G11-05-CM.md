---
task_id: G11-05-CM
project: clawmarketing
priority: 2
depends_on: []
modifies: ["src/api/endpoints/report_generation.py", "tests/test_report_generation.py"]
executor: glm
---
task_id: G11-05-CM  
project: clawmarketing  
priority: 2  
depends_on: []  
modifies: ["src/api/endpoints/report_generation.py", "tests/test_report_generation.py"]  

---
## 目标
实现社交媒体报告自动生成功能。

## 技术方案
1. 在 `report_generation.py` 中添加一个新的 API 端点 `/generate-report`，用于生成社交媒体报告。
2. 使用 `ReportLab` 库生成 PDF 格式的报告。
3. 在 `test_report_generation.py` 中编写测试，确保报告生成的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保报告生成功能的正确性。

## 验收标准
- [ ] API 端点 `/generate-report` 能够正确生成社交媒体报告。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。