---
task_id: G8-01-CM
project: clawmarketing
priority: 1
depends_on: []
modifies: ["api/routes/social_media.py", "api/models/post.py", "tests/test_social_media.py"]
executor: glm
---
task_id: G8-01-CM  
project: clawmarketing  
priority: 1  
depends_on: []  
modifies: ["api/routes/social_media.py", "api/models/post.py", "tests/test_social_media.py"]  

#### 目标
实现基于 AI 的社交媒体内容自动生成功能。

#### 技术方案
1. 集成 Hugging Face 的文本生成模型，通过 API 调用生成社交媒体内容。
2. 在 `social_media.py` 中添加新的路由 `/generate-content`，接收参数如平台类型和主题。
3. 在 `post.py` 中添加新的模型字段以存储生成的内容。
4. 编写基础测试，确保生成的内容符合预期格式和长度。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

#### 验收标准
- [ ] 新增的 `/generate-content` 路由返回生成的社交媒体内容。
- [ ] 生成的内容符合指定平台和主题的要求。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。