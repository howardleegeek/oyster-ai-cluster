---
task_id: G8-04-CM
project: clawmarketing
priority: 3
depends_on: []
modifies: ["api/services/notification.py", "api/models/notification.py", "tests/test_notification.py"]
executor: glm
---
task_id: G8-04-CM  
project: clawmarketing  
priority: 3  
depends_on: []  
modifies: ["api/services/notification.py", "api/models/notification.py", "tests/test_notification.py"]  

#### 目标
实现基于 AI 的个性化通知推送功能。

#### 技术方案
1. 在 `notification.py` 中添加新的服务方法，使用 AI 分析用户行为并生成个性化通知内容。
2. 更新 `notification` 模型，添加新的字段以存储个性化内容。
3. 集成第三方通知服务（如 Firebase Cloud Messaging）以发送通知。
4. 编写测试用例，验证通知内容的个性化和发送功能。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

#### 验收标准
- [ ] 新增的通知服务方法生成个性化内容。
- [ ] 通知内容通过第三方服务成功发送。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。