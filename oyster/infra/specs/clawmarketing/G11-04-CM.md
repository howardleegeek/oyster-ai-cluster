---
task_id: G11-04-CM
project: clawmarketing
priority: 3
depends_on: []
modifies: ["src/api/endpoints/user_management.py", "tests/test_user_management.py"]
executor: glm
---
task_id: G11-04-CM  
project: clawmarketing  
priority: 3  
depends_on: []  
modifies: ["src/api/endpoints/user_management.py", "tests/test_user_management.py"]  

---
## 目标
实现用户权限管理功能。

## 技术方案
1. 在 `user_management.py` 中添加新的 API 端点 `/manage-permissions`，用于管理用户的权限。
2. 使用 `SQLAlchemy` 进行数据库操作，确保权限的持久化存储。
3. 在 `test_user_management.py` 中编写测试，确保权限管理的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保权限管理的正确性。

## 验收标准
- [ ] API 端点 `/manage-permissions` 能够正确管理用户权限。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。