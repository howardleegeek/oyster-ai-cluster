---
task_id: G8-03-CM
project: clawmarketing
priority: 1
depends_on: []
modifies: ["api/routes/analytics.py", "api/models/analytics.py", "tests/test_analytics.py"]
executor: glm
---
task_id: G8-03-CM  
project: clawmarketing  
priority: 1  
depends_on: []  
modifies: ["api/routes/analytics.py", "api/models/analytics.py", "tests/test_analytics.py"]  

#### 目标
集成 AI 驱动的社交媒体数据分析功能。

#### 技术方案
1. 在 `analytics.py` 中添加新的路由 `/analyze-engagement`，接收社交媒体数据并返回分析结果。
2. 使用 AI 模型分析用户互动数据，识别趋势和潜在改进点。
3. 在 `analytics.py` 模型中添加新的字段以存储分析结果。
4. 编写测试用例，验证分析结果的准确性和完整性。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

#### 验收标准
- [ ] 新增的 `/analyze-engagement` 路由返回准确的 AI 分析结果。
- [ ] 分析结果包含趋势和潜在改进点。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。