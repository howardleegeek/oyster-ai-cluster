---
task_id: G11-02-CM
project: clawmarketing
priority: 2
depends_on: []
modifies: ["src/api/endpoints/automation.py", "tests/test_automation.py"]
executor: glm
---
task_id: G11-02-CM  
project: clawmarketing  
priority: 2  
depends_on: []  
modifies: ["src/api/endpoints/automation.py", "tests/test_automation.py"]  

---
## 目标
实现社交媒体帖子自动化发布。

## 技术方案
1. 在 `automation.py` 中添加一个新的 API 端点 `/schedule-post`，用于安排社交媒体帖子的发布时间。
2. 使用 `APScheduler` 库来处理定时任务。
3. 在 `test_automation.py` 中编写测试，确保帖子能够在预定时间发布。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保定时任务的正确性。

## 验收标准
- [ ] API 端点 `/schedule-post` 能够正确安排帖子发布时间。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。