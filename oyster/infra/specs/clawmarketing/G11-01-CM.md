---
task_id: G11-01-CM
project: clawmarketing
priority: 1
depends_on: []
modifies: ["src/api/endpoints/social_media.py", "tests/test_social_media.py"]
executor: glm
---
task_id: G11-01-CM  
project: clawmarketing  
priority: 1  
depends_on: []  
modifies: ["src/api/endpoints/social_media.py", "tests/test_social_media.py"]  

---
## 目标
实现社交媒体平台的基本数据抓取功能。

## 技术方案
1. 在 `social_media.py` 中添加一个新的 API 端点 `/fetch-data`，用于从 Twitter 和 Facebook 抓取数据。
2. 使用 `Tweepy` 和 `facebook-sdk` 库进行数据抓取。
3. 在 `test_social_media.py` 中编写基础测试，确保端点返回正确的数据格式。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保数据抓取功能的正确性。

## 验收标准
- [ ] API 端点 `/fetch-data` 返回正确的数据格式。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。