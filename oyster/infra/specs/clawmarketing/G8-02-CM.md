---
task_id: G8-02-CM
project: clawmarketing
priority: 2
depends_on: []
modifies: ["api/services/automation.py", "api/models/schedule.py", "tests/test_automation.py"]
executor: glm
---
task_id: G8-02-CM  
project: clawmarketing  
priority: 2  
depends_on: []  
modifies: ["api/services/automation.py", "api/models/schedule.py", "tests/test_automation.py"]  

#### 目标
增强社交媒体发布的自动化调度功能。

#### 技术方案
1. 在 `automation.py` 中添加新的调度算法，支持按用户时区自动调整发布时间。
2. 在 `schedule.py` 中添加时区字段，并更新数据库迁移脚本。
3. 更新现有的调度逻辑，确保在指定时间点准确发布内容。
4. 编写测试用例，验证不同时间区的调度功能。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

#### 验收标准
- [ ] 新增的调度算法根据用户时区调整发布时间。
- [ ] 数据库中 `schedule` 模型包含时区信息。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。