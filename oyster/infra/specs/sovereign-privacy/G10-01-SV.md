---
task_id: G10-01-SV
project: sovereign-privacy
priority: 1
depends_on: []
modifies: ["src/zk_prover/mod.rs", "src/zk_prover/constraints.rs"]
executor: glm
---
task_id: G10-01-SV  
project: sovereign-privacy  
priority: 1  
depends_on: []  
modifies: ["src/zk_prover/mod.rs", "src/zk_prover/constraints.rs"]  

---
## 目标
在 ZK 证明器中实现生产级的安全加固。

## 技术方案
- 在 `src/zk_prover/mod.rs` 中添加输入验证逻辑，确保所有输入在进入证明器之前都经过严格验证。
- 在 `src/zk_prover/constraints.rs` 中实现额外的约束条件，以防止潜在的恶意输入导致的安全漏洞。
- 使用 Rust 的安全特性，如 `unsafe` 代码块的最小化使用，确保代码的安全性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保输入验证和约束条件的正确性。

## 验收标准
- [ ] 所有输入在进入 ZK 证明器之前都经过验证。
- [ ] 额外的约束条件有效防止了潜在的恶意输入。
- [ ] 基础测试覆盖了输入验证和约束条件。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。