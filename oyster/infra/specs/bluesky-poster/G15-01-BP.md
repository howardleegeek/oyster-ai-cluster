---
task_id: G15-01-BP
project: bluesky-poster
priority: 1
depends_on: []
modifies: ["poster_scheduler.py", "tests/test_poster_scheduler.py"]
executor: glm
---
task_id: G15-01-BP  
project: bluesky-poster  
priority: 1  
depends_on: []  
modifies: ["poster_scheduler.py", "tests/test_poster_scheduler.py"]  

---  
## 目标  
实现基本的定时发布功能，确保帖子能按预定时间自动发布。  

## 技术方案  
1. 在 `poster_scheduler.py` 中使用 APScheduler 库来管理定时任务。  
2. 创建一个调度器实例，并添加发布任务的方法。  
3. 实现一个简单的 API 接口，允许用户设置帖子的发布时间。  
4. 在 `tests/test_poster_scheduler.py` 中编写测试用例，确保调度器能正确添加和执行任务。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保调度器功能正常。  

## 验收标准  
- [ ] 定时发布功能能按设定时间发布帖子。  
- [ ] API 接口能正确接收并设置发布时间。  
- [ ] pytest 全绿，测试覆盖主要功能路径。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。