---
task_id: G10-02-BP
project: bluesky-poster
priority: 2
depends_on: []
modifies: ["poster.py", "poster_utils.py", "tests/test_poster.py"]
executor: glm
---
task_id: G10-02-BP
project: bluesky-poster
priority: 2
depends_on: []
modifies: ["poster.py", "poster_utils.py", "tests/test_poster.py"]
---
## 目标
增强帖子内容的多样性，支持动态插入变量和模板。

## 技术方案
1. 在 `poster_utils.py` 中创建一个模板引擎模块，使用 `Jinja2` 来处理模板和变量。
2. 修改 `poster.py` 中的帖子发布逻辑，解析包含变量的模板并生成最终内容。
3. 在 `tests/test_poster.py` 中添加测试用例，验证模板渲染的正确性。
4. 更新文档，说明如何使用模板和变量。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保模板渲染功能正常。

## 验收标准
- [ ] 模板中变量能正确替换为实际值。
- [ ] 复杂模板结构能正确渲染。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。