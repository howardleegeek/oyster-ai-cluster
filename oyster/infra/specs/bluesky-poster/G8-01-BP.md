---
task_id: G8-01-BP
project: bluesky-poster
priority: 1
depends_on: []
modifies: ["poster_scheduler.py", "ai_integration.py"]
executor: glm
---
task_id: G8-01-BP  
project: bluesky-poster  
priority: 1  
depends_on: []  
modifies: ["poster_scheduler.py", "ai_integration.py"]  

---
## 目标
集成AI驱动的自动发帖功能，根据用户设定的时间表自动发布内容。

## 技术方案
1. 在 `poster_scheduler.py` 中添加一个调度器接口，用于接收AI生成的帖子内容。
2. 修改 `ai_integration.py`，引入一个AI模型API（如GPT-3.5）来生成符合用户主题的帖子内容。
3. 创建一个新的模块 `auto_posting.py`，负责将AI生成的帖子内容发送到Bluesky平台。
4. 确保调度器能够处理AI生成内容的异步请求，并在内容准备好后自动发布。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试，覆盖AI生成和自动发布的主要流程。

## 验收标准
- [ ] AI生成的帖子内容符合用户设定的主题。
- [ ] 帖子能够按照设定的时间表自动发布。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。