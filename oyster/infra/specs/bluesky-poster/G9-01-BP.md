---
task_id: G9-01-BP
project: bluesky-poster
priority: 1
depends_on: []
modifies: ["scheduler.py", "publisher.py", "tests/test_publisher.py"]
executor: glm
---
task_id: G9-01-BP
project: bluesky-poster
priority: 1
depends_on: []
modifies: ["scheduler.py", "publisher.py", "tests/test_publisher.py"]
---
## 目标
实现自动化内容发布与调度功能，允许用户预设发布时间。

## 技术方案
- 在 `scheduler.py` 中添加调度逻辑，使用 APScheduler 库来管理定时任务。
- 修改 `publisher.py` 以支持从调度器接收发布请求。
- 在 `tests/test_publisher.py` 中编写基础测试，确保调度与发布功能正常工作。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能可靠性。

## 验收标准
- [ ] 用户可以设置发布时间并成功保存调度任务。
- [ ] 定时任务在设定时间触发并发布内容。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。