---
task_id: G15-05-BP
project: bluesky-poster
priority: 2
depends_on: []
modifies: ["poster_scheduler.py", "poster_publisher.py", "tests/test_integration.py"]
executor: glm
---
task_id: G15-05-BP  
project: bluesky-poster  
priority: 2  
depends_on: []  
modifies: ["poster_scheduler.py", "poster_publisher.py", "tests/test_integration.py"]  

---  
## 目标  
实现调度器与发布器的集成，确保定时发布的帖子能自动发布到目标平台。  

## 技术方案  
1. 在 `poster_scheduler.py` 中调用 `poster_publisher.py` 中的发布器方法，实现定时任务的自动发布。  
2. 实现一个集成方法，接受调度器和发布器的实例，协调两者的交互。  
3. 在 `tests/test_integration.py` 中编写测试用例，验证调度器与发布器的集成功能是否正常。  
4. 添加日志记录功能，记录发布过程中的关键事件和错误。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保集成功能正常。  

## 验收标准  
- [ ] 定时发布的帖子能自动发布到目标平台。  
- [ ] 发布过程中的关键事件和错误被正确记录。  
- [ ] pytest 全绿，测试覆盖主要功能路径。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。