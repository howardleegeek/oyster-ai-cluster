---
task_id: G3-02-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["api/models/agent.py", "tests/test_models.py"]
executor: glm
---
task_id: G3-02-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["api/models/agent.py", "tests/test_models.py"]  

## 目标
定义Agent的基本数据模型。

## 技术方案
1. 在 `api/models/agent.py` 中创建一个Pydantic模型 `Agent`，包含必要的字段如 `id`, `name`, `description`, `created_at` 等。
2. 添加字段验证，确保每个字段的数据类型和格式正确。
3. 在 `tests/test_models.py` 中编写测试用例，验证模型的字段验证功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的pytest测试，确保模型验证功能正常。

## 验收标准
- [ ] Agent模型包含所有必要的字段。
- [ ] 每个字段的数据类型和格式验证正确。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。