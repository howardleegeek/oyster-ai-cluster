---
task_id: G15-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G15-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]  

## 目标
实现Agent的基本创建功能，允许用户通过低代码界面创建新的Agent实例。

## 技术方案
1. 在 `agent_manager.py` 中添加一个 `create_agent` 函数，负责处理Agent的创建逻辑。
2. 该函数将接收用户输入的参数（如名称、类型、配置），并将其存储在数据库中。
3. 更新相应的API端点以调用 `create_agent` 函数。
4. 在 `tests/test_agent_manager.py` 中编写基础测试，验证Agent创建功能是否正常。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正常。

## 验收标准
- [ ] 用户可以通过低代码界面成功创建新的Agent实例。
- [ ] API端点正确处理创建请求并返回成功响应。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。