---
task_id: G5-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["src/agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G5-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["src/agent_manager.py", "tests/test_agent_manager.py"]  

## 目标
实现多Agent的基础调度功能。

## 技术方案
- 在 `agent_manager.py` 中添加一个 `AgentScheduler` 类，负责管理Agent的启动、停止和调度。
- 使用 Python 的 `asyncio` 库实现异步调度。
- 在 `tests/test_agent_manager.py` 中编写基础测试，确保调度功能按预期工作。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正确。

## 验收标准
- [ ] Agent 可以被成功启动和停止。
- [ ] 调度功能可以按预定时间间隔运行。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。