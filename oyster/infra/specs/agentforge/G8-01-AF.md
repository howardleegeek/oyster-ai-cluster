---
task_id: G8-01-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G8-01-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]  

---

## 目标  
实现基于AI的智能调度功能，自动分配任务给空闲Agent。

## 技术方案  
1. 在 `agent_manager.py` 中添加一个 `schedule_task` 方法，使用预训练的AI模型分析Agent的当前负载和历史表现。  
2. 调用AI模型的API接口获取调度建议。  
3. 根据建议更新Agent的任务队列。  
4. 在 `tests/test_agent_manager.py` 中添加测试用例，验证调度逻辑的正确性。

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 写基础测试，确保调度功能按预期工作。

## 验收标准  
- [ ] AI调度建议被正确应用到Agent任务队列。  
- [ ] pytest 全绿，测试覆盖调度逻辑的主要路径。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。