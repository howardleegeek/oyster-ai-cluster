---
task_id: G8-04-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["task_execution.py", "tests/test_task_execution.py"]
executor: glm
---
task_id: G8-04-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["task_execution.py", "tests/test_task_execution.py"]  

---

## 目标  
引入任务执行失败自动重试机制，提高任务完成的成功率。

## 技术方案  
1. 在 `task_execution.py` 中添加重试逻辑，捕获任务执行中的异常。  
2. 实现指数退避算法，控制重试频率。  
3. 在 `tests/test_task_execution.py` 中添加测试用例，验证重试机制的有效性。  
4. 配置最大重试次数和超时时间，防止无限重试。

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 写基础测试，确保重试机制按预期工作。

## 验收标准  
- [ ] 任务执行失败时自动重试，并在达到最大重试次数后记录失败。  
- [ ] pytest 全绿，测试覆盖重试机制的主要路径。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。