---
task_id: G9-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G9-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]  
---  
## 目标  
实现基于角色的访问控制（RBAC）以增强平台安全性。  

## 技术方案  
1. 在 `agent_manager.py` 中添加 RBAC 模块，定义用户角色和权限。
2. 使用 FastAPI 的依赖注入系统来检查用户权限。
3. 在现有 API 端点中添加权限检查逻辑。
4. 编写单元测试以验证不同角色的访问权限。

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 写基础测试。  

## 验收标准  
- [ ] 不同角色的用户只能访问其权限范围内的资源。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。