---
task_id: G1-04-AF
project: agentforge
priority: 3
depends_on: []
modifies: ["src/api/endpoints/agents.py", "src/services/agent_service.py", "tests/test_agent_service.py"]
executor: glm
---
task_id: G1-04-AF  
project: agentforge  
priority: 3  
depends_on: []  
modifies: ["src/api/endpoints/agents.py", "src/services/agent_service.py", "tests/test_agent_service.py"]

---
## 目标
实现Agent更新功能。

## 技术方案
1. 在 `src/api/endpoints/agents.py` 中添加一个PUT接口，用于更新Agent信息。
2. 在 `src/services/agent_service.py` 中实现更新逻辑，确保数据正确性。
3. 在 `tests/test_agent_service.py` 中添加测试用例，验证更新操作的正确性和错误处理。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] API接口可以成功更新Agent信息。
- [ ] 更新操作遵循数据验证规则。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。