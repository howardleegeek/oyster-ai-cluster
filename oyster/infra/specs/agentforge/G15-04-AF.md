---
task_id: G15-04-AF
project: agentforge
priority: 3
depends_on: []
modifies: ["agent_monitor.py", "tests/test_agent_monitor.py"]
executor: glm
---
task_id: G15-04-AF  
project: agentforge  
priority: 3  
depends_on: []  
modifies: ["agent_monitor.py", "tests/test_agent_monitor.py"]  

## 目标
实现Agent的监控功能，实时跟踪Agent的运行状态和性能指标。

## 技术方案
1. 在 `agent_monitor.py` 中添加一个 `monitor_agent` 函数，负责收集和存储Agent的运行状态和性能数据。
2. 该函数将定期从Agent获取数据，并将其存储在数据库中。
3. 更新相应的API端点以提供监控数据。
4. 在 `tests/test_agent_monitor.py` 中编写基础测试，验证监控功能是否正常。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正常。

## 验收标准
- [ ] 用户可以实时查看Agent的运行状态和性能指标。
- [ ] API端点正确提供监控数据。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。