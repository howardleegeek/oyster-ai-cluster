---
task_id: G3-05-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["api/routes/agents.py", "tests/test_agents.py"]
executor: glm
---
task_id: G3-05-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["api/routes/agents.py", "tests/test_agents.py"]  

## 目标
实现Agent删除API端点。

## 技术方案
1. 在 `api/routes/agents.py` 中添加一个DELETE端点 `/agents/{agent_id}`，用于删除指定Agent。
2. 使用路径参数 `agent_id` 来定位要删除的Agent。
3. 在 `tests/test_agents.py` 中编写测试用例，验证端点能够正确删除Agent。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的pytest测试，确保删除功能正常。

## 验收标准
- [ ] DELETE `/agents/{agent_id}` 端点能够正确删除指定Agent。
- [ ] 返回的响应包含删除成功的消息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。