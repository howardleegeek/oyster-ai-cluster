---
task_id: G4-03-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["src/api/endpoints/agents.py", "tests/api/test_agents.py"]
executor: glm
---
task_id: G4-03-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["src/api/endpoints/agents.py", "tests/api/test_agents.py"]

## 目标
实现Agent的删除功能。

## 技术方案
1. 在 `agents.py` 中添加一个 DELETE 路由 `/api/agents/{agent_id}` 来处理 Agent 删除请求。
2. 编写相应的处理逻辑，验证 Agent 是否存在并执行删除操作。
3. 在 `test_agents.py` 中添加测试用例，覆盖成功删除和删除失败的情况。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保删除功能正常。

## 验收标准
- [ ] DELETE `/api/agents/{agent_id}` 接口返回 204 状态码。
- [ ] 尝试删除不存在的 Agent 时返回 404 状态码。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。