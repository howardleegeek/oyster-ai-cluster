---
task_id: G2-05-AF
project: agentforge
priority: 2
depends_on: ["G2-03-AF"]
modifies: ["api/routes/agent.py", "tests/test_agent.py"]
executor: glm
---
task_id: G2-05-AF  
project: agentforge  
priority: 2  
depends_on: ["G2-03-AF"]  
modifies: ["api/routes/agent.py", "tests/test_agent.py"]  

## 目标  
实现Agent的错误日志记录和查询功能，用户可以查看Agent运行中的错误日志。

## 技术方案  
1. 在 `api/routes/agent.py` 中添加API端点 `/agent/logs`，接收Agent的ID并返回其错误日志。  
2. 在 `agent_manager.py` 中添加日志记录功能，将错误信息写入日志文件。  
3. 在 `tests/test_agent.py` 中编写测试用例，验证日志查询功能是否正常。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保日志查询功能正常。  

## 验收标准  
- [ ] API端点 `/agent/logs` 正常响应。  
- [ ] 返回的错误日志信息准确。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。