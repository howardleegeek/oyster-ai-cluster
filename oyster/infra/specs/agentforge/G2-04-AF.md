---
task_id: G2-04-AF
project: agentforge
priority: 3
depends_on: ["G2-02-AF"]
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G2-04-AF  
project: agentforge  
priority: 3  
depends_on: ["G2-02-AF"]  
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]  

## 目标  
优化Agent的调度性能，确保在高并发情况下调度操作的响应时间。

## 技术方案  
1. 在 `agent_manager.py` 中引入异步调度机制，使用 `asyncio` 优化调度操作。  
2. 使用线程池或进程池来管理Agent的启动和停止操作。  
3. 在 `tests/test_agent_manager.py` 中编写性能测试用例，验证调度性能是否提升。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写性能测试，确保调度性能提升。  

## 验收标准  
- [ ] 调度操作的响应时间在高并发情况下显著减少。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。