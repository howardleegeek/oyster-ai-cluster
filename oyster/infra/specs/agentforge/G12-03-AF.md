---
task_id: G12-03-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["src/api/agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G12-03-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["src/api/agent_manager.py", "tests/test_agent_manager.py"]  

## 目标
实现Agent的状态查询功能。

## 技术方案
在 `agent_manager.py` 中添加一个 `get_agent_status` 方法，该方法接收Agent的ID并返回Agent的当前状态。使用FastAPI的依赖注入机制来管理Agent的状态查询。在 `test_agent_manager.py` 中编写测试用例，确保 `get_agent_status` 方法在接收到有效ID时返回正确的状态。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的单元测试，确保方法按预期工作。

## 验收标准
- [ ] `get_agent_status` 方法在接收到有效ID时返回200状态码和正确的状态。
- [ ] `get_agent_status` 方法在接收到无效ID时返回400状态码。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。