---
task_id: G3-03-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["api/routes/agents.py", "tests/test_agents.py"]
executor: glm
---
task_id: G3-03-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["api/routes/agents.py", "tests/test_agents.py"]  

## 目标
实现Agent列表查询API端点。

## 技术方案
1. 在 `api/routes/agents.py` 中添加一个GET端点 `/agents`，用于查询Agent列表。
2. 实现分页功能，支持 `page` 和 `page_size` 参数。
3. 在 `tests/test_agents.py` 中编写测试用例，验证端点能够正确返回Agent列表，并支持分页。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的pytest测试，确保查询功能正常。

## 验收标准
- [ ] GET `/agents` 端点能够正确返回Agent列表。
- [ ] 支持分页功能，能够根据 `page` 和 `page_size` 参数返回相应的数据。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。