---
task_id: G15-03-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["agent_executor.py", "tests/test_agent_executor.py"]
executor: glm
---
task_id: G15-03-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["agent_executor.py", "tests/test_agent_executor.py"]  

## 目标
实现Agent的执行功能，确保Agent按照调度设置运行。

## 技术方案
1. 在 `agent_executor.py` 中添加一个 `execute_agent` 函数，负责处理Agent的执行逻辑。
2. 该函数将根据调度设置调用相应的Agent执行逻辑。
3. 更新相应的API端点以触发执行操作。
4. 在 `tests/test_agent_executor.py` 中编写基础测试，验证执行功能是否正常。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正常。

## 验收标准
- [ ] Agent能够按照调度设置自动运行。
- [ ] API端点正确触发执行操作并返回成功响应。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。