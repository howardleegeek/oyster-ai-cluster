---
task_id: G4-04-AF
project: agentforge
priority: 3
depends_on: []
modifies: ["src/api/endpoints/agents.py", "tests/api/test_agents.py"]
executor: glm
---
task_id: G4-04-AF  
project: agentforge  
priority: 3  
depends_on: []  
modifies: ["src/api/endpoints/agents.py", "tests/api/test_agents.py"]

## 目标
实现Agent列表的获取功能。

## 技术方案
1. 在 `agents.py` 中添加一个 GET 路由 `/api/agents` 来处理 Agent 列表请求。
2. 编写相应的处理逻辑，从数据库中获取 Agent 列表并返回。
3. 在 `test_agents.py` 中添加测试用例，覆盖成功获取列表和空列表的情况。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保列表获取功能正常。

## 验收标准
- [ ] GET `/api/agents` 接口返回 200 状态码和 Agent 列表。
- [ ] 当没有 Agent 时返回空列表。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。