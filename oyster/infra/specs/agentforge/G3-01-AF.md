---
task_id: G3-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["api/routes/agents.py", "tests/test_agents.py"]
executor: glm
---
task_id: G3-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["api/routes/agents.py", "tests/test_agents.py"]  

## 目标
实现基本的Agent创建API端点。

## 技术方案
1. 在 `api/routes/agents.py` 中添加一个POST端点 `/agents`，用于接收Agent的创建请求。
2. 使用FastAPI的Pydantic模型来验证请求数据。
3. 在 `tests/test_agents.py` 中编写测试用例，确保端点能够正确创建Agent并返回预期的响应。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的pytest测试，确保API端点功能正常。

## 验收标准
- [ ] POST `/agents` 端点能够正确创建Agent。
- [ ] 返回的响应包含新创建的Agent的详细信息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。