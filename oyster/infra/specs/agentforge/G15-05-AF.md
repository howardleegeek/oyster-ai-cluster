---
task_id: G15-05-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["agent_configurator.py", "tests/test_agent_configurator.py"]
executor: glm
---
task_id: G15-05-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["agent_configurator.py", "tests/test_agent_configurator.py"]  

## 目标
实现Agent的配置功能，允许用户通过低代码界面配置Agent的参数。

## 技术方案
1. 在 `agent_configurator.py` 中添加一个 `configure_agent` 函数，负责处理Agent的配置逻辑。
2. 该函数将接收用户输入的配置参数，并将其存储在数据库中。
3. 更新相应的API端点以调用 `configure_agent` 函数。
4. 在 `tests/test_agent_configurator.py` 中编写基础测试，验证配置功能是否正常。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正常。

## 验收标准
- [ ] 用户可以通过界面成功配置Agent的参数。
- [ ] API端点正确处理配置请求并返回成功响应。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。