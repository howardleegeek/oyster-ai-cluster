---
task_id: G12-06-AF
project: agentforge
priority: 3
depends_on: ["G12-01-AF", "G12-03-AF"]
modifies: ["src/api/agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G12-06-AF  
project: agentforge  
priority: 3  
depends_on: ["G12-01-AF", "G12-03-AF"]  
modifies: ["src/api/agent_manager.py", "tests/test_agent_manager.py"]  

## 目标
实现Agent的错误处理和异常上报功能。

## 技术方案
在 `agent_manager.py` 中添加一个 `handle_agent_errors` 方法，该方法接收Agent的ID并处理Agent运行中的异常。使用FastAPI的异常处理机制来捕获和处理异常。在 `test_agent_manager.py` 中编写测试用例，确保 `handle_agent_errors` 方法在接收到有效ID时能够正确处理异常并上报。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的单元测试，确保方法按预期工作。

## 验收标准
- [ ] `handle_agent_errors` 方法在接收到有效ID时能够正确处理异常并返回200状态码。
- [ ] `handle_agent_errors` 方法在接收到无效ID时返回400状态码。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。