---
task_id: G3-04-AF
project: agentforge
priority: 3
depends_on: []
modifies: ["api/routes/agents.py", "tests/test_agents.py"]
executor: glm
---
task_id: G3-04-AF  
project: agentforge  
priority: 3  
depends_on: []  
modifies: ["api/routes/agents.py", "tests/test_agents.py"]  

## 目标
实现Agent更新API端点。

## 技术方案
1. 在 `api/routes/agents.py` 中添加一个PUT端点 `/agents/{agent_id}`，用于更新指定Agent的信息。
2. 使用路径参数 `agent_id` 来定位要更新的Agent。
3. 在 `tests/test_agents.py` 中编写测试用例，验证端点能够正确更新Agent的信息。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的pytest测试，确保更新功能正常。

## 验收标准
- [ ] PUT `/agents/{agent_id}` 端点能够正确更新指定Agent的信息。
- [ ] 返回的响应包含更新后的Agent的详细信息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。