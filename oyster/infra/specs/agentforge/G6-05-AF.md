---
task_id: G6-05-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["agent_executor.py", "tests/test_agent_executor.py"]
executor: glm
---
task_id: G6-05-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["agent_executor.py", "tests/test_agent_executor.py"]
---
## 目标
实现Agent任务的自动重试机制，提高任务执行的可靠性。

## 技术方案
1. 在 `agent_executor.py` 中添加重试逻辑，定义重试次数和间隔。
2. 实现指数退避算法，动态调整重试间隔。
3. 在 `tests/test_agent_executor.py` 中编写测试，确保重试机制在不同失败情况下的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保新功能稳定。

## 验收标准
- [ ] 失败的任务能够自动重试，并在成功时继续执行。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。