---
task_id: G2-02-AF
project: agentforge
priority: 2
depends_on: ["G2-01-AF"]
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G2-02-AF  
project: agentforge  
priority: 2  
depends_on: ["G2-01-AF"]  
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]  

## 目标  
实现Agent的基本调度功能，能够根据用户配置启动和停止Agent。

## 技术方案  
1. 在 `agent_manager.py` 中实现 `AgentManager` 类，负责管理Agent的生命周期。  
2. 添加方法 `start_agent` 和 `stop_agent`，分别用于启动和停止Agent。  
3. 在 `tests/test_agent_manager.py` 中编写测试用例，验证调度功能是否正常。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保调度功能正常。  

## 验收标准  
- [ ] `start_agent` 方法能够成功启动Agent。  
- [ ] `stop_agent` 方法能够成功停止Agent。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。