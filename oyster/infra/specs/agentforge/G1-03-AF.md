---
task_id: G1-03-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["src/api/endpoints/agents.py", "tests/test_agents.py"]
executor: glm
---
task_id: G1-03-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["src/api/endpoints/agents.py", "tests/test_agents.py"]

---
## 目标
实现Agent删除功能。

## 技术方案
1. 在 `src/api/endpoints/agents.py` 中添加一个DELETE接口，用于删除指定Agent。
2. 实现删除逻辑，确保数据一致性。
3. 在 `tests/test_agents.py` 中添加测试用例，验证删除操作的成功和失败场景。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] API接口可以成功删除指定的Agent。
- [ ] 删除不存在的Agent时返回正确的错误信息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。