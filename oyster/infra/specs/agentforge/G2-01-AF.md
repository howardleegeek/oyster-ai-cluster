---
task_id: G2-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
executor: glm
---
task_id: G2-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["main.py", "tests/test_main.py"]  

## 目标  
实现基础的Agent创建功能，允许用户通过低代码方式创建和配置Agent。

## 技术方案  
1. 在 `main.py` 中添加一个API端点 `/create_agent`，接收Agent的基本配置参数。  
2. 使用FastAPI的Pydantic模型验证输入数据。  
3. 在 `tests/test_main.py` 中编写测试用例，验证API端点的功能是否正常。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保API端点功能正常。  

## 验收标准  
- [ ] API端点 `/create_agent` 正常响应。  
- [ ] 输入参数验证正确，错误输入返回相应的错误信息。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。