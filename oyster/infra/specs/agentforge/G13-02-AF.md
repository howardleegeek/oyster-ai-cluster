---
task_id: G13-02-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["agent_scheduler.py", "tests/test_agent_scheduler.py"]
executor: glm
---
task_id: G13-02-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["agent_scheduler.py", "tests/test_agent_scheduler.py"]  

## 目标
实现Agent的调度功能，支持定时任务。

## 技术方案
1. 在 `agent_scheduler.py` 中使用 APScheduler 库来管理定时任务。
2. 添加 `schedule_agent_task` 和 `unschedule_agent_task` 方法。
3. 在 `tests/test_agent_scheduler.py` 中编写测试用例，验证调度和取消调度功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保调度功能正确。

## 验收标准
- [ ] Agent任务可以按预定时间执行。
- [ ] 任务可以成功取消。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。