---
task_id: G11-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G11-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
---
## 目标
实现Agent的基本生命周期管理功能，包括启动、停止和重启。

## 技术方案
- 在 `agent_manager.py` 中添加 `start_agent`, `stop_agent`, 和 `restart_agent` 方法。
- 使用 FastAPI 的依赖注入机制来管理Agent的状态。
- 在 `tests/test_agent_manager.py` 中编写相应的单元测试，确保每个方法按预期工作。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] Agent可以成功启动、停止和重启
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS