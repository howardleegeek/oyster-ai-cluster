---
task_id: G15-02-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["agent_scheduler.py", "tests/test_agent_scheduler.py"]
executor: glm
---
task_id: G15-02-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["agent_scheduler.py", "tests/test_agent_scheduler.py"]  

## 目标
实现Agent的调度功能，允许用户设置Agent的运行时间和频率。

## 技术方案
1. 在 `agent_scheduler.py` 中添加一个 `schedule_agent` 函数，负责处理Agent的调度逻辑。
2. 该函数将接收用户设置的调度参数（如时间、频率），并将其存储在数据库中。
3. 更新相应的API端点以调用 `schedule_agent` 函数。
4. 在 `tests/test_agent_scheduler.py` 中编写基础测试，验证调度功能是否正常。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正常。

## 验收标准
- [ ] 用户可以通过界面成功设置Agent的运行时间和频率。
- [ ] API端点正确处理调度请求并返回成功响应。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。