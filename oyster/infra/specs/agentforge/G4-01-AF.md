---
task_id: G4-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["src/api/endpoints/agents.py", "tests/api/test_agents.py"]
executor: glm
---
task_id: G4-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["src/api/endpoints/agents.py", "tests/api/test_agents.py"]

## 目标
实现基础的Agent创建API接口。

## 技术方案
1. 在 `agents.py` 中添加一个 POST 路由 `/api/agents` 来接收 Agent 创建请求。
2. 编写相应的请求处理逻辑，验证输入数据并保存到数据库。
3. 在 `test_agents.py` 中添加测试用例，覆盖成功创建和输入验证失败的场景。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保接口功能正常。

## 验收标准
- [ ] POST `/api/agents` 接口返回 201 状态码并包含新 Agent 的信息。
- [ ] 输入验证失败时返回 400 状态码和相应的错误信息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。