---
task_id: G5-05-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["src/agent_logging.py", "tests/test_agent_logging.py"]
executor: glm
---
task_id: G5-05-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["src/agent_logging.py", "tests/test_agent_logging.py"]  

## 目标
实现Agent的日志记录和监控。

## 技术方案
- 在 `agent_logging.py` 中集成 Python 的 `logging` 模块，实现日志的集中管理和记录。
- 支持将日志输出到控制台、文件，并支持日志级别控制。
- 在 `tests/test_agent_logging.py` 中编写测试，验证日志记录的正确性。
- 实现基本的日志监控功能，实时查看Agent的运行状态。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写日志测试，确保日志记录和监控功能可靠。

## 验收标准
- [ ] 日志可以按预期记录到控制台和文件。
- [ ] 日志级别控制有效。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。