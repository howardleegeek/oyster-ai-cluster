---
task_id: G12-05-AF
project: agentforge
priority: 2
depends_on: ["G12-03-AF"]
modifies: ["src/api/agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G12-05-AF  
project: agentforge  
priority: 2  
depends_on: ["G12-03-AF"]  
modifies: ["src/api/agent_manager.py", "tests/test_agent_manager.py"]  

## 目标
实现Agent日志查询功能。

## 技术方案
在 `agent_manager.py` 中添加一个 `get_agent_logs` 方法，该方法接收Agent的ID和时间范围并返回相应的日志信息。使用FastAPI的依赖注入机制来管理日志查询。在 `test_agent_manager.py` 中编写测试用例，确保 `get_agent_logs` 方法在接收到有效参数时返回正确的日志信息。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的单元测试，确保方法按预期工作。

## 验收标准
- [ ] `get_agent_logs` 方法在接收到有效参数时返回200状态码和正确的日志信息。
- [ ] `get_agent_logs` 方法在接收到无效参数时返回400状态码。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。