---
task_id: G2-03-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["api/routes/agent.py", "tests/test_agent.py"]
executor: glm
---
task_id: G2-03-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["api/routes/agent.py", "tests/test_agent.py"]  

## 目标  
实现Agent的实时状态查询功能，用户可以实时获取Agent的运行状态。

## 技术方案  
1. 在 `api/routes/agent.py` 中添加API端点 `/agent/status`，接收Agent的ID并返回其状态。  
2. 使用FastAPI的异步特性，确保查询操作的实时性。  
3. 在 `tests/test_agent.py` 中编写测试用例，验证状态查询功能是否正常。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保状态查询功能正常。  

## 验收标准  
- [ ] API端点 `/agent/status` 正常响应。  
- [ ] 返回的Agent状态信息准确。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。