---
task_id: G14-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]
executor: glm
---
task_id: G14-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["agent_manager.py", "tests/test_agent_manager.py"]  

## 目标
实现多Agent并发执行的调度机制。

## 技术方案
1. 在 `agent_manager.py` 中引入线程池或异步任务队列。
2. 修改现有 Agent 执行逻辑以支持并发调度。
3. 编写基础测试以验证并发执行的安全性。
4. 更新 `tests/test_agent_manager.py` 以包含并发执行的单元测试。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] 多个 Agent 可以同时执行而不会互相干扰。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。