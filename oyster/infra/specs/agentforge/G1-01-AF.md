---
task_id: G1-01-AF
project: agentforge
priority: 1
depends_on: []
modifies: ["src/api/endpoints/agents.py", "tests/test_agents.py"]
executor: glm
---
task_id: G1-01-AF  
project: agentforge  
priority: 1  
depends_on: []  
modifies: ["src/api/endpoints/agents.py", "tests/test_agents.py"]

---
## 目标
实现基础Agent创建API接口。

## 技术方案
1. 在 `src/api/endpoints/agents.py` 中添加一个POST接口，用于接收Agent创建请求。
2. 编写相应的数据模型和验证逻辑。
3. 在 `tests/test_agents.py` 中添加测试用例，覆盖成功创建和错误处理场景。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] API接口可以成功创建Agent。
- [ ] API接口可以正确处理错误输入。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。