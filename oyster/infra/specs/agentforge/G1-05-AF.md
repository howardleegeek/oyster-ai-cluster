---
task_id: G1-05-AF
project: agentforge
priority: 2
depends_on: []
modifies: ["src/api/endpoints/agents.py", "src/services/agent_service.py", "tests/test_agent_service.py"]
executor: glm
---
task_id: G1-05-AF  
project: agentforge  
priority: 2  
depends_on: []  
modifies: ["src/api/endpoints/agents.py", "src/services/agent_service.py", "tests/test_agent_service.py"]

---
## 目标
实现Agent详情查询功能。

## 技术方案
1. 在 `src/api/endpoints/agents.py` 中添加一个GET接口，用于查询指定Agent的详细信息。
2. 在 `src/services/agent_service.py` 中实现查询逻辑，确保高效的数据检索。
3. 在 `tests/test_agent_service.py` 中添加测试用例，验证查询结果的正确性和性能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] API接口可以返回正确的Agent详细信息。
- [ ] 查询性能满足预期。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。