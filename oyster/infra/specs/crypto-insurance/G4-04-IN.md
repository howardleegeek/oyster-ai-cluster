---
task_id: G4-04-IN
project: crypto-insurance
priority: 3
depends_on: ["G4-01-IN", "G4-03-IN"]
modifies: ["src/ai_risk_scoring/api.py", "tests/test_api.py"]
executor: glm
---
task_id: G4-04-IN  
project: crypto-insurance  
priority: 3  
depends_on: ["G4-01-IN", "G4-03-IN"]  
modifies: ["src/ai_risk_scoring/api.py", "tests/test_api.py"]  
---  
## 目标  
构建 AI 风险评分 API 接口。  

## 技术方案  
1. 在 `api.py` 中使用 Flask 创建一个 RESTful API，用于接收用户数据并返回风险评分。  
2. 实现数据验证和错误处理。  
3. 在 `test_api.py` 中编写测试，验证 API 的响应和错误处理。  

## 约束  
- 在现有代码库内修改，不新建超过 3 个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保 API 的正确性和稳定性。  

## 验收标准  
- [ ] API 能够接收用户数据并返回风险评分。  
- [ ] API 对无效输入进行错误处理。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。