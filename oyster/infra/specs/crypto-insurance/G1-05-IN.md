---
task_id: G1-05-IN
project: crypto-insurance
priority: 3
depends_on: []
modifies: ["src/frontend/components/ClaimForm.jsx", "src/frontend/App.jsx"]
executor: glm
---
task_id: G1-05-IN  
project: crypto-insurance  
priority: 3  
depends_on: []  
modifies: ["src/frontend/components/ClaimForm.jsx", "src/frontend/App.jsx"]  
---  
## 目标  
在用户界面中实现索赔提交表单，允许用户提交索赔请求。  

## 技术方案  
- 在 `ClaimForm.jsx` 中创建索赔提交表单，包括索赔金额和描述等输入字段。  
- 添加表单验证，确保用户输入的数据有效。  
- 在 `App.jsx` 中集成索赔表单组件，并处理表单提交事件，调用后端 API 提交索赔。  
- 使用 React 状态管理来显示提交结果和错误信息。  

## 约束  
- 仅修改 `ClaimForm.jsx` 和 `App.jsx` 文件，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础的 Jest 测试，验证索赔提交逻辑。  

## 验收标准  
- [ ] 用户可以输入索赔金额和描述并提交表单。  
- [ ] 表单验证正确工作，阻止无效输入。  
- [ ] 提交成功后显示成功消息。  
- [ ] Jest 测试全绿。  

## 不要做  
- 不重构现有 UI 组件。  
- 不修改 CSS 样式。