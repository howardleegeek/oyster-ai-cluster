---
task_id: G5-04-IN
project: crypto-insurance
priority: 3
depends_on: ["G5-01-IN", "G5-02-IN"]
modifies: ["src/api.py", "tests/test_api.py"]
executor: glm
---
task_id: G5-04-IN
project: crypto-insurance
priority: 3
depends_on: ["G5-01-IN", "G5-02-IN"]
modifies: ["src/api.py", "tests/test_api.py"]
---
## 目标
实现保险计算和风险评估的 API 接口。

## 技术方案
在 `api.py` 中添加两个端点 `/calculate_premium` 和 `/assess_risk`，分别调用 `calculate_premium` 和 `assess_risk` 函数。编写对应的单元测试，确保 API 端点的正确性和稳定性。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] API 端点能够正确处理请求并返回计算结果和风险评分
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS