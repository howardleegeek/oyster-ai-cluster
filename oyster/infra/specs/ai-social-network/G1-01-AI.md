---
task_id: G1-01-AI
project: ai-social-network
priority: 1
depends_on: []
modifies: ["app/main.py", "app/models/user.py", "tests/test_user.py"]
executor: glm
---
task_id: G1-01-AI
project: ai-social-network
priority: 1
depends_on: []
modifies: ["app/main.py", "app/models/user.py", "tests/test_user.py"]
---
## 目标
实现用户注册和登录的基础功能。

## 技术方案
1. 在 `app/main.py` 中添加用户注册和登录的 API 端点。
2. 在 `app/models/user.py` 中定义用户模型，包括用户名、密码哈希和邮箱字段。
3. 使用 Python 的 `bcrypt` 库对用户密码进行哈希处理。
4. 在 `tests/test_user.py` 中编写测试用例，验证用户注册和登录的 API 端点。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保用户注册和登录功能正常。

## 验收标准
- [ ] 用户可以通过 API 注册新账户。
- [ ] 用户可以通过 API 使用注册账户登录。
- [ ] 密码在数据库中以哈希形式存储。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。