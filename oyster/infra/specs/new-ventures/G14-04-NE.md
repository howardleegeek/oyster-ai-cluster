---
task_id: G14-04-NE
project: new-ventures
priority: 3
depends_on: []
modifies: ["main.py", "utils.py"]
executor: glm
---
task_id: G14-04-NE  
project: new-ventures  
priority: 3  
depends_on: []  
modifies: ["main.py", "utils.py"]  

#### 目标
实现用户数据导出功能。

#### 技术方案
- 在 `main.py` 中添加新的路由 `/export-data`，处理数据导出请求。
- 在 `utils.py` 中实现数据导出逻辑，支持 CSV 和 JSON 格式。
- 返回导出的数据文件作为响应。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，验证导出功能的正确性和性能。

#### 验收标准
- [ ] 用户可以通过 GET 请求导出数据。
- [ ] 导出成功后，返回 200 状态码和相应的文件。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。