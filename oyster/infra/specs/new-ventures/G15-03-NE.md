---
task_id: G15-03-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
executor: glm
---
task_id: G15-03-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "tests/test_main.py"]  

#### 目标
实现用户资料获取 API。

#### 技术方案
1. 在 `main.py` 中添加一个 `/profile` 路由，使用 JWT 认证保护该路由。
2. 从数据库中获取当前用户的资料信息，并返回给客户端。
3. 在 `tests/test_main.py` 中编写测试用例，验证资料获取功能。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保资料获取功能正常。

#### 验收标准
- [ ] 资料获取 API 返回 200 状态码和用户资料。
- [ ] 未认证的请求返回 401 状态码。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。