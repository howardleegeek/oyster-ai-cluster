---
task_id: G15-05-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "models/user.py", "tests/test_main.py"]
executor: glm
---
task_id: G15-05-NE  
project: new-ventures  
priority: 2  
depends_on: []  
modifies: ["main.py", "models/user.py", "tests/test_main.py"]  

#### 目标
实现用户密码重置功能。

#### 技术方案
1. 在 `main.py` 中添加一个 `/reset-password` 路由，处理密码重置请求。
2. 在 `models/user.py` 中添加密码重置逻辑，生成重置令牌并发送邮件。
3. 在 `tests/test_main.py` 中编写测试用例，验证密码重置功能。
4. 确保密码重置链接的安全性，使用一次性令牌。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保密码重置功能正常。

#### 验收标准
- [ ] 密码重置请求返回 200 状态码并发送重置邮件。
- [ ] 使用无效的重置令牌返回 400 状态码。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。