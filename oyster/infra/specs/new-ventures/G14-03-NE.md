---
task_id: G14-03-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "schemas.py"]
executor: glm
---
task_id: G14-03-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "schemas.py"]  

#### 目标
添加用户登录的 API 端点。

#### 技术方案
- 在 `main.py` 中添加新的路由 `/login`，处理用户登录请求。
- 在 `schemas.py` 中定义 `UserLogin` 数据模型，包含用户名和密码字段。
- 使用 JWT 生成认证令牌，并返回给客户端。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保登录功能的安全性和正确性。

#### 验收标准
- [ ] 用户可以通过 POST 请求提交登录信息。
- [ ] 登录成功后，返回 200 状态码和 JWT 令牌。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。