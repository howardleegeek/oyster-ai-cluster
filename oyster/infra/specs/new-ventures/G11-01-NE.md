---
task_id: G11-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
executor: glm
---
task_id: G11-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
---
## 目标
实现用户注册功能的基础版本。

## 技术方案
1. 在 `main.py` 中添加一个新的 API 端点 `/register`，使用 FastAPI 的 `HTTP POST` 方法。
2. 在 `models.py` 中定义一个新的数据模型 `UserRegistration`，包含用户名、密码和邮箱字段。
3. 在 `tests/test_main.py` 中编写基础的单元测试，验证注册端点的响应状态和返回数据。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保注册功能的基本功能正常。

## 验收标准
- [ ] API 端点 `/register` 返回 200 状态码和成功消息。
- [ ] 用户数据正确存储到数据库中。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。