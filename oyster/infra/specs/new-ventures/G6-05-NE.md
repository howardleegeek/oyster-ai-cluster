---
task_id: G6-05-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["auth.py", "tests/test_auth.py"]
executor: glm
---
task_id: G6-05-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["auth.py", "tests/test_auth.py"]
---
## 目标
增强用户认证和授权机制，以支持更细粒度的权限控制。

## 技术方案
1. 使用 OAuth2 和 JWT 来实现更安全的用户认证。
2. 在 `auth.py` 中实现新的认证和授权逻辑。
3. 修改现有的路由装饰器以支持基于角色的访问控制。
4. 编写单元测试以验证新的认证和授权机制。
5. 在 `tests/test_auth.py` 中添加相应的测试用例。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保新的认证和授权机制能够正确工作。

## 验收标准
- [ ] 用户能够使用 OAuth2 登录并获得 JWT 令牌。
- [ ] pytest 全绿，确保新功能不影响现有功能。
- [ ] 基于角色的访问控制在所有相关端点中正确实施。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。