---
task_id: G3-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
executor: glm
---
task_id: G3-01-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "models.py", "tests/test_main.py"]

---
## 目标
实现用户注册功能的基础版本。

## 技术方案
1. 在 `main.py` 中添加一个 `/register` 端点，使用 FastAPI 的路由功能。
2. 在 `models.py` 中定义一个 `User` 数据模型，包含用户名、密码和邮箱字段。
3. 使用 Pydantic 进行数据验证，确保输入数据的有效性。
4. 在 `tests/test_main.py` 中编写测试用例，验证注册端点的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保注册功能的基本功能正常。

## 验收标准
- [ ] 用户可以通过 `/register` 端点注册新账号。
- [ ] 注册时输入的邮箱格式正确。
- [ ] 密码长度符合要求（至少8个字符）。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。