---
task_id: G5-03-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["app/main.py", "app/models.py", "tests/test_main.py"]
executor: glm
---
task_id: G5-03-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["app/main.py", "app/models.py", "tests/test_main.py"]  
---  
## 目标  
实现用户信息获取功能的基本接口。  

## 技术方案  
1. 在 `app/main.py` 中添加一个 `/user/{user_id}` 端点，使用 FastAPI 的 `APIRouter` 进行路由管理。  
2. 在 `app/models.py` 中定义一个 `User` 模型，包含用户的基本信息字段。  
3. 在 `tests/test_main.py` 中编写基础测试，验证用户信息获取接口的响应状态码和数据格式。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保接口功能正常。  

## 验收标准  
- [ ] 获取用户信息接口返回 200 状态码和用户数据。  
- [ ] 获取用户信息接口返回 404 状态码和错误消息。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。