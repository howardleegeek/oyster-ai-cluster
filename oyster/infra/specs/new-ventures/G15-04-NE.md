---
task_id: G15-04-NE
project: new-ventures
priority: 3
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
executor: glm
---
task_id: G15-04-NE  
project: new-ventures  
priority: 3  
depends_on: []  
modifies: ["main.py", "tests/test_main.py"]  

#### 目标
实现用户登出 API。

#### 技术方案
1. 在 `main.py` 中添加一个 `/logout` 路由，使用 JWT 认证保护该路由。
2. 实现登出逻辑，清除客户端的 JWT（如果适用）或使 JWT 无效。
3. 在 `tests/test_main.py` 中编写测试用例，验证登出功能。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保登出功能正常。

#### 验收标准
- [ ] 登出 API 返回 200 状态码。
- [ ] 登出后，使用相同的 JWT 请求受保护的资源返回 401 状态码。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。