---
task_id: G15-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
executor: glm
---
task_id: G15-01-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "tests/test_main.py"]  

#### 目标
实现用户注册 API 的基础功能。

#### 技术方案
1. 在 `main.py` 中添加一个 `/register` 路由，使用 FastAPI 的 `APIRouter`。
2. 创建对应的 Pydantic 模型用于验证用户输入数据。
3. 在 `tests/test_main.py` 中编写测试用例，确保注册 API 的基本功能正常。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保注册功能正常。

#### 验收标准
- [ ] 注册 API 返回 201 状态码和用户信息。
- [ ] 注册时提供重复的用户名，返回 400 状态码。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。