---
task_id: G1-03-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "utils.py", "tests/test_utils.py"]
executor: glm
---
task_id: G1-03-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "utils.py", "tests/test_utils.py"]
---
## 目标
实现用户输入数据的验证和清理。

## 技术方案
1. 在 `utils.py` 中创建数据验证和清理函数，例如检查邮箱格式和密码强度。
2. 在 `main.py` 中调用这些函数，在处理用户注册请求之前验证和清理输入数据。
3. 在 `tests/test_utils.py` 中编写测试用例，验证验证和清理函数的功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保验证和清理功能正常。

## 验收标准
- [ ] 输入数据通过验证后，才能进行后续处理。
- [ ] 无效数据被正确拒绝，并返回相应的错误信息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。