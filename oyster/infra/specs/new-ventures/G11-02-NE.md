---
task_id: G11-02-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "schemas.py", "tests/test_main.py"]
executor: glm
---
task_id: G11-02-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "schemas.py", "tests/test_main.py"]
---
## 目标
实现用户登录功能的基础版本。

## 技术方案
1. 在 `main.py` 中添加一个新的 API 端点 `/login`，使用 FastAPI 的 `HTTP POST` 方法。
2. 在 `schemas.py` 中定义一个新的请求体模式 `UserLogin`，包含用户名和密码字段。
3. 在 `tests/test_main.py` 中编写基础的单元测试，验证登录端点的响应状态和返回数据。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保登录功能的基本功能正常。

## 验收标准
- [ ] API 端点 `/login` 返回 200 状态码和 JWT 令牌。
- [ ] 验证用户凭证的正确性。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。