---
task_id: G5-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["app/main.py", "app/models.py", "tests/test_main.py"]
executor: glm
---
task_id: G5-01-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["app/main.py", "app/models.py", "tests/test_main.py"]  
---  
## 目标  
实现用户注册功能的基本接口。  

## 技术方案  
1. 在 `app/main.py` 中添加一个 `/register` 端点，使用 FastAPI 的 `APIRouter` 进行路由管理。  
2. 在 `app/models.py` 中定义一个 `UserRegistration` 模型，包含用户名、密码和邮箱字段。  
3. 在 `tests/test_main.py` 中编写基础测试，验证注册接口的响应状态码和数据格式。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保接口功能正常。  

## 验收标准  
- [ ] 注册接口返回 200 状态码和成功消息。  
- [ ] 注册接口返回 400 状态码和错误消息。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。