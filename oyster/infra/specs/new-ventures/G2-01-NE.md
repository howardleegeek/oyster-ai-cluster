---
task_id: G2-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
executor: glm
---
task_id: G2-01-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "models.py", "tests/test_main.py"]  
---  
## 目标  
实现用户注册功能的基础版本。  

## 技术方案  
1. 在 `main.py` 中添加一个 `/register` 端点，接收用户名、邮箱和密码。  
2. 在 `models.py` 中创建 `User` 数据模型，包含必要的字段和验证。  
3. 在 `tests/test_main.py` 中编写测试用例，验证用户注册的成功和失败场景。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保注册功能的基本正确性。  

## 验收标准  
- [ ] 用户可以通过 `/register` 端点成功注册。  
- [ ] 注册时提供重复的用户名或邮箱会返回错误。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。