---
task_id: G2-05-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "models.py", "tests/test_models.py"]
executor: glm
---
task_id: G2-05-NE  
project: new-ventures  
priority: 2  
depends_on: []  
modifies: ["main.py", "models.py", "tests/test_models.py"]  
---  
## 目标  
实现用户数据的验证和清理。  

## 技术方案  
1. 在 `models.py` 中添加数据验证逻辑，确保用户输入的数据符合预期格式。  
2. 在 `main.py` 中修改相关端点，调用数据验证逻辑。  
3. 在 `tests/test_models.py` 中编写测试用例，验证数据验证和清理功能。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保数据验证和清理的正确性。  

## 验收标准  
- [ ] 用户输入的数据被正确验证和清理。  
- [ ] 无效的数据输入会返回相应的错误信息。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。