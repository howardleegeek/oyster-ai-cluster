---
task_id: G12-02-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "models/user.py", "tests/test_main.py"]
executor: glm
---
task_id: G12-02-NE  
project: new-ventures  
priority: 2  
depends_on: []  
modifies: ["main.py", "models/user.py", "tests/test_main.py"]  

## 目标  
为用户登录功能添加 JWT 认证。

## 技术方案  
在 FastAPI 应用中创建一个新的 POST 端点 `/login`，验证用户凭证并生成 JWT 令牌。使用 `PyJWT` 库生成和验证 JWT 令牌，并在受保护的路由中验证 JWT。

## 约束  
- 在现有代码库内修改，不新建超过3个文件  
- 不留 TODO/FIXME/placeholder  
- 写基础测试，覆盖成功登录和认证失败的情况  

## 验收标准  
- [ ] POST `/login` 端点返回 200 状态码和 JWT 令牌  
- [ ] 受保护的路由在未提供有效 JWT 时返回 401 状态码  
- [ ] pytest 全绿  

## 不要做  
- 不重构现有代码  
- 不改 UI/CSS