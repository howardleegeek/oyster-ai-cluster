---
task_id: G11-05-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "schemas.py", "tests/test_main.py"]
executor: glm
---
task_id: G11-05-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "schemas.py", "tests/test_main.py"]
---
## 目标
实现用户搜索功能的基础版本。

## 技术方案
1. 在 `main.py` 中添加一个新的 API 端点 `/search_users`，使用 FastAPI 的 `HTTP GET` 方法。
2. 在 `schemas.py` 中定义一个新的请求体模式 `UserSearch`，包含搜索关键词和过滤条件。
3. 在 `tests/test_main.py` 中编写基础的单元测试，验证搜索端点的响应状态和返回数据。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保搜索功能的基本功能正常。

## 验收标准
- [ ] API 端点 `/search_users` 返回 200 状态码和搜索结果。
- [ ] 搜索结果符合预期的过滤条件。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。