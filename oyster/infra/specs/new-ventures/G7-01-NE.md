---
task_id: G7-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
executor: glm
---
task_id: G7-01-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "models.py", "tests/test_main.py"]  

## 目标
集成一个基于 AI 的推荐系统，以提高用户参与度。

## 技术方案
1. 在 `main.py` 中添加一个 API 端点 `/recommend`，用于接收用户输入并返回推荐结果。
2. 在 `models.py` 中实现一个简单的推荐算法，使用基于内容的过滤方法。
3. 在 `tests/test_main.py` 中编写测试用例，验证推荐端点的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保推荐系统按预期工作。

## 验收标准
- [ ] API 端点 `/recommend` 返回有效的推荐结果。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。