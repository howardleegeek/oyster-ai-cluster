---
task_id: G2-02-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "database.py", "tests/test_database.py"]
executor: glm
---
task_id: G2-02-NE  
project: new-ventures  
priority: 2  
depends_on: []  
modifies: ["main.py", "database.py", "tests/test_database.py"]  
---  
## 目标  
实现用户数据的持久化存储。  

## 技术方案  
1. 在 `database.py` 中配置数据库连接，使用 SQLite 作为初始数据库。  
2. 修改 `main.py` 中的注册端点，将用户数据保存到数据库中。  
3. 在 `tests/test_database.py` 中编写测试用例，验证用户数据是否正确存储和检索。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保数据存储和检索的正确性。  

## 验收标准  
- [ ] 用户注册后，数据被正确保存到数据库中。  
- [ ] 通过数据库查询，可以正确检索用户数据。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。