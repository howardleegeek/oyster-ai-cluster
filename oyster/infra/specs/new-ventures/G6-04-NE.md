---
task_id: G6-04-NE
project: new-ventures
priority: 3
depends_on: []
modifies: ["models.py", "tests/test_models.py"]
executor: glm
---
task_id: G6-04-NE
project: new-ventures
priority: 3
depends_on: []
modifies: ["models.py", "tests/test_models.py"]
---
## 目标
优化数据库模型以支持更复杂的数据关系和查询。

## 技术方案
1. 使用 SQLAlchemy 的关系功能来定义更复杂的数据关系（如多对多关系）。
2. 优化现有的查询以利用新的关系和索引。
3. 在 `models.py` 中添加新的模型和关系。
4. 编写单元测试以验证新的数据关系和查询功能。
5. 在 `tests/test_models.py` 中添加相应的测试用例。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保新的数据关系和查询功能正常工作。

## 验收标准
- [ ] 新增的数据关系能够正确反映业务逻辑。
- [ ] pytest 全绿，确保新功能不影响现有功能。
- [ ] 查询性能在预期范围内（不超过 2 秒对于复杂查询）。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。