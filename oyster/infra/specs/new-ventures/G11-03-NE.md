---
task_id: G11-03-NE
project: new-ventures
priority: 3
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
executor: glm
---
task_id: G11-03-NE
project: new-ventures
priority: 3
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
---
## 目标
实现用户资料更新的基础版本。

## 技术方案
1. 在 `main.py` 中添加一个新的 API 端点 `/update_profile`，使用 FastAPI 的 `HTTP PUT` 方法。
2. 在 `models.py` 中更新 `User` 数据模型，添加可更新的字段，如姓名、地址等。
3. 在 `tests/test_main.py` 中编写基础的单元测试，验证更新端点的响应状态和返回数据。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保更新功能的基本功能正常。

## 验收标准
- [ ] API 端点 `/update_profile` 返回 200 状态码和成功消息。
- [ ] 用户资料正确更新到数据库中。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。