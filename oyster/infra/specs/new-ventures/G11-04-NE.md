---
task_id: G11-04-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
executor: glm
---
task_id: G11-04-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
---
## 目标
实现用户删除功能的基础版本。

## 技术方案
1. 在 `main.py` 中添加一个新的 API 端点 `/delete_user`，使用 FastAPI 的 `HTTP DELETE` 方法。
2. 在 `models.py` 中添加删除用户的方法，确保相关数据的一致性。
3. 在 `tests/test_main.py` 中编写基础的单元测试，验证删除端点的响应状态和数据库中的用户数据是否被正确删除。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保删除功能的基本功能正常。

## 验收标准
- [ ] API 端点 `/delete_user` 返回 200 状态码和成功消息。
- [ ] 用户数据从数据库中正确删除。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。