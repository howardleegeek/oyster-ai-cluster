---
task_id: G4-05-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "models/order.py", "tests/test_main.py"]
executor: glm
---
task_id: G4-05-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "models/order.py", "tests/test_main.py"]
---
## 目标
实现基本的订单创建功能。

## 技术方案
1. 在 `models/order.py` 中定义 `Order` 数据模型，包含用户ID、商品ID、数量和总价。
2. 在 `main.py` 中添加一个 `/create_order` 端点，接受订单详情作为请求体。
3. 实现订单数据的验证和保存到数据库的逻辑。
4. 在 `tests/test_main.py` 中编写测试用例，验证订单创建的正确性和错误处理。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] 用户可以成功创建订单并收到确认响应。
- [ ] 提供无效的订单详情时返回适当的错误信息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。