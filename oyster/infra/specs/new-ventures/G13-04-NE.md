---
task_id: G13-04-NE
project: new-ventures
priority: 3
depends_on: []
modifies: ["main.py", "models/order.py", "tests/test_main.py"]
executor: glm
---
task_id: G13-04-NE  
project: new-ventures  
priority: 3  
depends_on: []  
modifies: ["main.py", "models/order.py", "tests/test_main.py"]  
---  
## 目标  
实现用户下单功能的基础版本。  

## 技术方案  
1. 在 `main.py` 中添加新的路由 `/place_order` 来处理用户下单请求。  
2. 在 `models/order.py` 中定义订单的数据模型，并实现订单创建和存储的逻辑。  
3. 在 `tests/test_main.py` 中添加测试用例，验证下单接口的正常工作和订单创建。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保下单接口的正确性。  

## 验收标准  
- [ ] 用户可以通过 `/place_order` 路由成功下单。  
- [ ] 下单时提供无效数据时，接口返回适当的错误信息。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。