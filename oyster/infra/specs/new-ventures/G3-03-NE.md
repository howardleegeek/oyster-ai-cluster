---
task_id: G3-03-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "database.py", "tests/test_main.py"]
executor: glm
---
task_id: G3-03-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "database.py", "tests/test_main.py"]

---
## 目标
实现用户数据的基础存储功能。

## 技术方案
1. 在 `database.py` 中配置数据库连接，使用 SQLAlchemy 作为 ORM。
2. 在 `main.py` 中更新用户相关的端点，使其能够与数据库交互。
3. 在 `models.py` 中定义用户表的模型。
4. 在 `tests/test_main.py` 中编写测试用例，验证用户数据的存储和检索。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保数据存储功能的基本功能正常。

## 验收标准
- [ ] 用户注册后，数据被正确存储在数据库中。
- [ ] 用户登录时，数据库中的用户数据被正确检索。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。