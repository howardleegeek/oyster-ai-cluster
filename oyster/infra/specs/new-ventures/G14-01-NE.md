---
task_id: G14-01-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["main.py", "models.py"]
executor: glm
---
task_id: G14-01-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["main.py", "models.py"]  

#### 目标
实现用户注册功能的 API 端点。

#### 技术方案
- 在 `main.py` 中添加新的路由 `/register`，使用 FastAPI 的 `APIRouter`。
- 在 `models.py` 中定义 `UserRegistration` 数据模型，包含用户名、密码和邮箱字段。
- 使用 Pydantic 进行数据验证，并调用现有的用户服务逻辑进行注册处理。

#### 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保注册功能按预期工作。

#### 验收标准
- [ ] 用户可以通过 POST 请求提交注册信息。
- [ ] 注册成功后返回 201 状态码和用户信息。
- [ ] pytest 全绿。

#### 不要做
- 不重构现有代码。
- 不改 UI/CSS。