---
task_id: G3-05-NE
project: new-ventures
priority: 2
depends_on: []
modifies: ["main.py", "services.py", "tests/test_main.py"]
executor: glm
---
task_id: G3-05-NE  
project: new-ventures  
priority: 2  
depends_on: []  
modifies: ["main.py", "services.py", "tests/test_main.py"]

---
## 目标
实现用户密码重置功能的基础版本。

## 技术方案
1. 在 `services.py` 中创建一个密码重置服务，处理密码重置逻辑。
2. 在 `main.py` 中添加一个 `/reset-password` 端点，使用 FastAPI 的路由功能。
3. 在 `models.py` 中更新用户模型，添加密码重置相关的字段。
4. 在 `tests/test_main.py` 中编写测试用例，验证密码重置端点的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保密码重置功能的基本功能正常。

## 验收标准
- [ ] 用户可以通过 `/reset-password` 端点请求密码重置。
- [ ] 密码重置链接通过电子邮件发送给用户。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。