---
task_id: G7-03-NE
project: new-ventures
priority: 1
depends_on: []
modifies: ["controllers/user_controller.py", "tests/test_user_controller.py"]
executor: glm
---
task_id: G7-03-NE  
project: new-ventures  
priority: 1  
depends_on: []  
modifies: ["controllers/user_controller.py", "tests/test_user_controller.py"]  

## 目标
增强用户认证流程的安全性。

## 技术方案
1. 在 `controllers/user_controller.py` 中添加 JWT 认证逻辑。
2. 更新用户登录和注册端点，以生成和验证 JWT 令牌。
3. 在 `tests/test_user_controller.py` 中编写测试用例，验证认证流程的正确性。

## 约束
- 在现有代码库内修改，不新建超过2个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保认证流程的安全性。

## 验收标准
- [ ] JWT 令牌在用户登录后正确生成。
- [ ] 认证端点在提供有效令牌时允许访问。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。