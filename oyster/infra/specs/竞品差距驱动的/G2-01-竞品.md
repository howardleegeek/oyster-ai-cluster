---
task_id: G2-01-竞品
project: 竞品差距驱动的
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
executor: glm
---
task_id: G2-01-竞品
project: 竞品差距驱动的
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
---
## 目标
实现基本的API端点以支持用户注册功能。

## 技术方案
在 `main.py` 中添加一个 `/register` 端点，使用 FastAPI 的路由功能来处理 POST 请求。实现用户数据的简单验证和存储逻辑。编写相应的单元测试以验证端点的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 编写基础的单元测试，确保注册功能正常工作

## 验收标准
- [ ] `/register` 端点能够接收用户数据并返回成功响应
- [ ] 用户数据能够正确存储（假设使用内存存储）
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS