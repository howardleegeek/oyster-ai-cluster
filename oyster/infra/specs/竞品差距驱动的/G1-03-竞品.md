---
task_id: G1-03-竞品
project: 竞品差距驱动的
priority: 1
depends_on: []
modifies: ["api/endpoints/product.py", "tests/test_product.py"]
executor: glm
---
task_id: G1-03-竞品
project: 竞品差距驱动的
priority: 1
depends_on: []
modifies: ["api/endpoints/product.py", "tests/test_product.py"]
---
## 目标
实现产品列表的获取功能，支持分页和排序。

## 技术方案
1. 在 `api/endpoints/product.py` 中添加一个 GET 端点 `/products`，接收分页和排序参数。
2. 使用 FastAPI 的查询参数功能处理分页和排序。
3. 在 `tests/test_product.py` 中添加测试用例，验证分页和排序功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] 产品列表端点正确处理分页和排序参数。
- [ ] 返回的产品列表符合分页和排序要求。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。