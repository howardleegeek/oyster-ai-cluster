---
task_id: G2-03-竞品
project: 竞品差距驱动的
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
executor: glm
---
task_id: G2-03-竞品
project: 竞品差距驱动的
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
---
## 目标
优化API端点的性能，确保高并发下的稳定性。

## 技术方案
在 `main.py` 中使用异步路由处理请求，并使用 Gunicorn 部署 FastAPI 应用以支持多进程。编写负载测试脚本，模拟高并发请求，并进行性能调优。在 `tests/test_main.py` 中添加性能测试用例。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 编写基础的负载和性能测试

## 验收标准
- [ ] API端点在1000并发请求下响应时间不超过100ms
- [ ] 负载测试无错误响应
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS