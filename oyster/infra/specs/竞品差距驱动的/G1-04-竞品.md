---
task_id: G1-04-竞品
project: 竞品差距驱动的
priority: 3
depends_on: []
modifies: ["api/endpoints/order.py", "tests/test_order.py"]
executor: glm
---
task_id: G1-04-竞品
project: 竞品差距驱动的
priority: 3
depends_on: []
modifies: ["api/endpoints/order.py", "tests/test_order.py"]
---
## 目标
实现订单创建功能，支持多种支付方式。

## 技术方案
1. 在 `api/endpoints/order.py` 中添加一个 POST 端点 `/orders`，接收订单信息和支付方式。
2. 验证订单信息的有效性。
3. 调用相应的支付接口处理支付。
4. 在 `tests/test_order.py` 中添加测试用例，验证订单创建和支付处理。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] 订单创建端点正确处理有效的订单信息。
- [ ] 支持多种支付方式。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。