---
task_id: G2-04-竞品
project: 竞品差距驱动的
priority: 3
depends_on: ["G2-01-竞品", "G2-02-竞品"]
modifies: ["main.py", "services/auth_service.py", "tests/test_auth_service.py"]
executor: glm
---
task_id: G2-04-竞品
project: 竞品差距驱动的
priority: 3
depends_on: ["G2-01-竞品", "G2-02-竞品"]
modifies: ["main.py", "services/auth_service.py", "tests/test_auth_service.py"]
---
## 目标
实现用户密码重置功能。

## 技术方案
在 `main.py` 中添加一个 `/reset_password` 端点，处理密码重置请求。在 `services/auth_service.py` 中实现密码重置逻辑，包括发送重置邮件和更新密码功能。编写相应的单元测试以验证密码重置功能的正确性。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 编写基础的单元测试，确保密码重置功能正常工作

## 验收标准
- [ ] `/reset_password` 端点能够接收重置请求并发送重置邮件
- [ ] 用户能够通过重置链接成功更新密码
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS