---
task_id: G3-02-竞品
project: 竞品差距驱动的
priority: 2
depends_on: ["G3-01-竞品"]
modifies: ["api/main.py", "api/utils.py", "tests/test_main.py"]
executor: glm
---
task_id: G3-02-竞品  
project: 竞品差距驱动的  
priority: 2  
depends_on: ["G3-01-竞品"]  
modifies: ["api/main.py", "api/utils.py", "tests/test_main.py"]  
---  
## 目标  
优化 API 响应时间，提高整体性能。  

## 技术方案  
使用 `async` 和 `await` 关键字优化数据库查询和外部 API 调用。引入缓存机制，使用 `aiocache` 库缓存常用数据。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件  
- 不留 TODO/FIXME/placeholder  
- 编写缓存相关的单元测试  

## 验收标准  
- [ ] 关键 API 端点的平均响应时间减少 30%  
- [ ] 缓存命中率达到预期水平  
- [ ] pytest 全绿  

## 不要做  
- 不重构现有代码  
- 不改 UI/CSS