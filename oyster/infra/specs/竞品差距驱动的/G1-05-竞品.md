---
task_id: G1-05-竞品
project: 竞品差距驱动的
priority: 2
depends_on: []
modifies: ["api/endpoints/auth.py", "tests/test_auth.py"]
executor: glm
---
task_id: G1-05-竞品
project: 竞品差距驱动的
priority: 2
depends_on: []
modifies: ["api/endpoints/auth.py", "tests/test_auth.py"]
---
## 目标
实现用户登出功能，失效 JWT 令牌。

## 技术方案
1. 在 `api/endpoints/auth.py` 中添加一个 POST 端点 `/logout`，接收 JWT 令牌。
2. 将令牌添加到黑名单中，标记为失效。
3. 在 `tests/test_auth.py` 中添加测试用例，验证登出功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] 登出端点正确处理有效的 JWT 令牌。
- [ ] 令牌被标记为失效。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。