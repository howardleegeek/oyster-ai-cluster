---
task_id: G1-05-AI
project: ai-driven-parametric-crypto-in
priority: 2
depends_on: []
modifies: ["api/utils/logging.py", "api/routes/insurance.py"]
executor: glm
---
task_id: G1-05-AI  
project: ai-driven-parametric-crypto-in  
priority: 2  
depends_on: []  
modifies: ["api/utils/logging.py", "api/routes/insurance.py"]  
---  
## 目标  
添加详细的日志记录，方便后续的监控和问题排查。  

## 技术方案  
1. 在 `api/utils/logging.py` 中配置日志记录器，记录请求参数、计算过程和结果。  
2. 在 `api/routes/insurance.py` 的 `/calculate-premium` 端点中调用日志记录函数，记录每个请求的详细信息。  
3. 确保日志信息包含时间戳、请求 ID、输入参数、计算结果和错误信息（如果有）。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 写基础测试，验证日志记录功能的正确性。  

## 验收标准  
- [ ] 日志记录器正确记录每个请求的详细信息。  
- [ ] pytest 全绿，确保日志记录功能正常。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。