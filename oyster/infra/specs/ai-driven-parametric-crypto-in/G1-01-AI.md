---
task_id: G1-01-AI
project: ai-driven-parametric-crypto-in
priority: 1
depends_on: []
modifies: ["api/routes/insurance.py", "api/models/parametric_model.py"]
executor: glm
---
task_id: G1-01-AI  
project: ai-driven-parametric-crypto-in  
priority: 1  
depends_on: []  
modifies: ["api/routes/insurance.py", "api/models/parametric_model.py"]  
---  
## 目标  
实现基础的参数化保险计算接口。  

## 技术方案  
1. 在 `api/routes/insurance.py` 中添加一个 POST 端点 `/calculate-premium`，接收参数化保险的输入数据。  
2. 在 `api/models/parametric_model.py` 中实现一个简单的计算模型，根据输入参数计算保费。  
3. 确保接口返回保费计算结果和相关信息。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 写基础测试，验证保费计算的准确性。  

## 验收标准  
- [ ] POST 请求 `/calculate-premium` 返回正确的保费计算结果。  
- [ ] pytest 全绿，确保新增接口和模型功能正常。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。