---
task_id: G1-03-AI
project: ai-driven-parametric-crypto-in
priority: 1
depends_on: []
modifies: ["api/models/parametric_model.py", "tests/test_models.py"]
executor: glm
---
task_id: G1-03-AI  
project: ai-driven-parametric-crypto-in  
priority: 1  
depends_on: []  
modifies: ["api/models/parametric_model.py", "tests/test_models.py"]  
---  
## 目标  
优化参数化保险计算模型，提高计算性能。  

## 技术方案  
1. 在 `api/models/parametric_model.py` 中重构计算模型，使用更高效的算法或数据结构。  
2. 使用缓存机制（如 Redis）存储中间计算结果，减少重复计算。  
3. 在 `tests/test_models.py` 中添加性能测试，确保优化后的模型性能提升。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 写基础测试，验证模型优化后的正确性和性能。  

## 验收标准  
- [ ] 模型计算性能提升至少 20%。  
- [ ] pytest 全绿，确保优化后的模型功能正常。  

## 不要做  
- 不重构现有代码（除非是为了优化性能）。  
- 不改 UI/CSS。