---
task_id: G4-01-NO
project: next-gen-oracles
priority: 1
depends_on: []
modifies: ["data_verification.py", "tests/test_data_verification.py"]
executor: glm
---
task_id: G4-01-NO
project: next-gen-oracles
priority: 1
depends_on: []
modifies: ["data_verification.py", "tests/test_data_verification.py"]
---
## 目标
实现基本的链下数据验证功能，确保数据来源的可靠性。

## 技术方案
1. 在 `data_verification.py` 中添加函数 `verify_data_source`，使用 Python 的 `requests` 库获取数据并验证其哈希值。
2. 实现简单的哈希验证逻辑，确保数据未被篡改。
3. 在 `tests/test_data_verification.py` 中编写单元测试，覆盖成功和失败的验证场景。
4. 使用 `pytest` 运行测试，确保所有测试通过。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正确性。

## 验收标准
- [ ] `verify_data_source` 函数能够正确验证数据来源的哈希值。
- [ ] 单元测试覆盖成功和失败的验证场景。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。