---
task_id: G4-02-NO
project: next-gen-oracles
priority: 2
depends_on: []
modifies: ["cross_chain_verification.py", "tests/test_cross_chain_verification.py"]
executor: glm
---
task_id: G4-02-NO
project: next-gen-oracles
priority: 2
depends_on: []
modifies: ["cross_chain_verification.py", "tests/test_cross_chain_verification.py"]
---
## 目标
实现基本的跨链数据验证功能，确保数据在不同区块链之间的一致性。

## 技术方案
1. 在 `cross_chain_verification.py` 中添加函数 `verify_cross_chain_data`，使用 Web3.py 库连接不同的区块链节点。
2. 实现跨链数据一致性检查逻辑，确保数据在不同链上的状态一致。
3. 在 `tests/test_cross_chain_verification.py` 中编写单元测试，覆盖成功和失败的跨链验证场景。
4. 使用 `pytest` 运行测试，确保所有测试通过。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正确性。

## 验收标准
- [ ] `verify_cross_chain_data` 函数能够正确验证数据在不同链上的状态。
- [ ] 单元测试覆盖成功和失败的跨链验证场景。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。