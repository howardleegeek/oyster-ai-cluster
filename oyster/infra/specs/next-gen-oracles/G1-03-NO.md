---
task_id: G1-03-NO
project: next-gen-oracles
priority: 1
depends_on: []
modifies: ["data_sources.py", "tests/test_data_sources.py"]
executor: glm
---
task_id: G1-03-NO
project: next-gen-oracles
priority: 1
depends_on: []
modifies: ["data_sources.py", "tests/test_data_sources.py"]
---
## 目标
扩展支持多种数据源。

## 技术方案
1. 在 `data_sources.py` 中创建一个类 `DataSource`，并实现不同数据源的子类，如 `WebDataSource`, `IPFSDataSource`。
2. 为每个数据源子类实现 `fetch_data` 方法，用于获取数据。
3. 在 `tests/test_data_sources.py` 中编写测试用例，覆盖不同数据源的获取和数据验证。
4. 更新 `data_verification.py` 以支持多种数据源。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保功能正确性。

## 验收标准
- [ ] 支持至少三种不同的数据源。
- [ ] 测试用例覆盖所有新增的数据源。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。