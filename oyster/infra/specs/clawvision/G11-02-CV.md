---
task_id: G11-02-CV
project: clawvision
priority: 2
depends_on: []
modifies: ["app/routes/vision.py", "app/services/image_processing.py", "tests/test_vision.py"]
executor: glm
---
task_id: G11-02-CV
project: clawvision
priority: 2
depends_on: []
modifies: ["app/routes/vision.py", "app/services/image_processing.py", "tests/test_vision.py"]
---
## 目标
集成基本的 AI 图像分析功能。

## 技术方案
1. 在 `app/routes/vision.py` 中添加一个 POST 端点 `/analyze` 来接收图像并进行 AI 分析。
2. 使用 `FastAPI` 的 `Depends` 功能调用 `app/services/image_processing.py` 中的图像处理函数。
3. 在 `app/services/image_processing.py` 中集成一个简单的 AI 模型（如预训练的图像分类模型）来进行图像分析。
4. 在 `tests/test_vision.py` 中编写测试用例，确保 AI 分析功能正常工作。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保 AI 分析功能正常。

## 验收标准
- [ ] 上传图像后，AI 分析结果正确返回。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。