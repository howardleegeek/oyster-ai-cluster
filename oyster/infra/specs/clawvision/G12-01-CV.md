---
task_id: G12-01-CV
project: clawvision
priority: 1
depends_on: []
modifies: ["src/api/routes/image_processing.py", "tests/test_image_processing.py"]
executor: glm
---
task_id: G12-01-CV  
project: clawvision  
priority: 1  
depends_on: []  
modifies: ["src/api/routes/image_processing.py", "tests/test_image_processing.py"]  

---
## 目标
实现基于 AI 的图像自动裁剪功能。

## 技术方案
在 FastAPI 的 `image_processing` 路由中添加一个新的端点，调用 AI 模型进行图像裁剪，并返回裁剪后的图像。使用 OpenCV 进行图像处理，并在后端进行初步验证。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试，确保裁剪功能正常

## 验收标准
- [ ] 新的 `/api/image/crop` 端点存在并能正常工作
- [ ] 裁剪后的图像符合预期尺寸和比例
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS