---
task_id: G14-02-CV
project: clawvision
priority: 2
depends_on: ["G14-01-CV"]
modifies: ["api/routes/image_processing.py", "tests/test_image_processing.py"]
executor: glm
---
task_id: G14-02-CV  
project: clawvision  
priority: 2  
depends_on: ["G14-01-CV"]  
modifies: ["api/routes/image_processing.py", "tests/test_image_processing.py"]  

---
## 目标
实现图像处理功能，包括图像裁剪和调整大小。

## 技术方案
1. 在 `api/routes/image_processing.py` 中添加一个 POST 端点 `/process-image`，接收图像文件和处理参数。
2. 使用 Pillow 库进行图像裁剪和调整大小。
3. 返回处理后的图像文件。
4. 在 `tests/test_image_processing.py` 中编写测试用例，验证处理功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保处理功能正常。

## 验收标准
- [ ] 接收图像文件和处理参数并返回处理后的图像。
- [ ] 裁剪图像至指定尺寸。
- [ ] 调整图像至指定大小。
- [ ] 处理过程中出现错误时返回错误信息。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。