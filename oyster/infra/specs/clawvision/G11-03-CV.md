---
task_id: G11-03-CV
project: clawvision
priority: 1
depends_on: []
modifies: ["app/main.py", "app/models.py", "tests/test_main.py"]
executor: glm
---
task_id: G11-03-CV
project: clawvision
priority: 1
depends_on: []
modifies: ["app/main.py", "app/models.py", "tests/test_main.py"]
---
## 目标
实现图像的元数据提取和存储。

## 技术方案
1. 在 `app/main.py` 中扩展现有的 `/upload` 端点，添加对图像元数据的提取。
2. 使用 Python 的 `Pillow` 库提取图像的尺寸、格式等元数据。
3. 将提取的元数据存储在数据库中，并在 `app/models.py` 中更新数据库模型以包含元数据字段。
4. 在 `tests/test_main.py` 中编写测试用例，确保元数据提取和存储功能正常工作。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保元数据提取和存储功能正常。

## 验收标准
- [ ] 上传图像后，图像的元数据被正确提取和存储。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。