---
task_id: G12-02-CV
project: clawvision
priority: 2
depends_on: []
modifies: ["src/api/routes/image_analysis.py", "tests/test_image_analysis.py"]
executor: glm
---
task_id: G12-02-CV  
project: clawvision  
priority: 2  
depends_on: []  
modifies: ["src/api/routes/image_analysis.py", "tests/test_image_analysis.py"]  

---
## 目标
集成 AI 模型以分析图像中的主要颜色和元素。

## 技术方案
在 FastAPI 的 `image_analysis` 路由中添加功能，调用预训练的 AI 模型来分析图像的主要颜色和元素。将结果以 JSON 格式返回，并在后端进行数据验证。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试，确保分析结果准确

## 验收标准
- [ ] 新的颜色和元素分析功能在 `/api/image/analyze` 端点中可用
- [ ] 返回的 JSON 数据包含主要颜色和元素的信息
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS