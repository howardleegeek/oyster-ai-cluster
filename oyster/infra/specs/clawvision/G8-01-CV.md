---
task_id: G8-01-CV
project: clawvision
priority: 2
depends_on: []
modifies: ["app/routers/image_processor.py", "tests/test_image_processor.py"]
executor: glm
---
task_id: G8-01-CV  
project: clawvision  
priority: 2  
depends_on: []  
modifies: ["app/routers/image_processor.py", "tests/test_image_processor.py"]  

## 目标
实现基于AI的自动图像裁剪功能。

## 技术方案
利用FastAPI的路由功能，添加一个新的API端点`/api/v1/auto-crop`，该端点接收图像数据并调用AI模型进行自动裁剪。修改现有的`image_processor.py`以集成新的裁剪逻辑，并编写相应的单元测试。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保新功能按预期工作。

## 验收标准
- [ ] 新增的`/api/v1/auto-crop`端点能够正确接收图像并返回裁剪后的图像。
- [ ] AI模型在测试图像上裁剪结果符合预期。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。