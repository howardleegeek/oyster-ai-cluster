---
task_id: G11-05-CV
project: clawvision
priority: 2
depends_on: []
modifies: ["app/main.py", "app/services/security.py", "tests/test_security.py"]
executor: glm
---
task_id: G11-05-CV
project: clawvision
priority: 2
depends_on: []
modifies: ["app/main.py", "app/services/security.py", "tests/test_security.py"]
---
## 目标
增强平台的安全性。

## 技术方案
1. 在 `app/main.py` 中使用 `FastAPI` 的安全功能，添加对 JWT 认证的支持。
2. 在 `app/services/security.py` 中实现 JWT 认证逻辑，包括生成和验证 JWT 令牌。
3. 在所有需要认证的端点中添加认证中间件。
4. 在 `tests/test_security.py` 中编写测试用例，确保认证功能正常工作。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保认证功能正常。

## 验收标准
- [ ] 只有经过认证的用户才能访问受保护的端点。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。