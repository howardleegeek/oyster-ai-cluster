---
task_id: G5-04-PP
project: prediction-perps
priority: 3
depends_on: ["G5-01-PP", "G5-02-PP"]
modifies: ["src/contracts/PredictionMarket.sol", "tests/test_prediction_market.py"]
executor: glm
---
task_id: G5-04-PP
project: prediction-perps
priority: 3
depends_on: ["G5-01-PP", "G5-02-PP"]
modifies: ["src/contracts/PredictionMarket.sol", "tests/test_prediction_market.py"]
---
## 目标
实现智能合约的安全性增强功能。

## 技术方案
1. 在 `PredictionMarket.sol` 中添加权限控制，确保只有授权的用户可以创建市场和结算合约。
2. 实现防止重入攻击的机制，使用 `reentrancy_guard` 装饰器。
3. 在 `test_prediction_market.py` 中编写安全性测试，覆盖权限控制和防止重入攻击的功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础的安全性测试，确保安全性功能被覆盖。

## 验收标准
- [ ] 只有授权的用户可以创建市场和结算合约。
- [ ] 智能合约防止重入攻击。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。