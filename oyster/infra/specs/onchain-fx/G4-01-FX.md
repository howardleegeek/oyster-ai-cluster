---
task_id: G4-01-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/Stablecoin.sol", "tests/test_stablecoin.py"]
executor: glm
---
task_id: G4-01-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/Stablecoin.sol", "tests/test_stablecoin.py"]
---
## 目标
实现多货币稳定币之间的原子交换功能。

## 技术方案
在 `Stablecoin.sol` 中添加一个 `swap` 函数，使用 Solidity 的 `call` 和 `delegatecall` 实现不同稳定币之间的原子交换。更新对应的测试用例，确保在 `tests/test_stablecoin.py` 中覆盖交换功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] 不同稳定币之间可以成功交换
- [ ] 交换过程中没有资金损失
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS