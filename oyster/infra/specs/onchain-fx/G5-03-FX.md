---
task_id: G5-03-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/MultiCurrency.sol", "tests/test_multi_currency.py"]
executor: glm
---
task_id: G5-03-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/MultiCurrency.sol", "tests/test_multi_currency.py"]
---
## 目标
实现多货币稳定币的铸造和销毁功能。

## 技术方案
1. 在 `MultiCurrency.sol` 中添加 `mint` 和 `burn` 函数，支持用户铸造和销毁多货币稳定币。
2. 实现铸造和销毁操作的权限控制，确保只有授权用户可以执行。
3. 在 `tests/test_multi_currency.py` 中添加测试用例，验证铸造和销毁功能的正确性。
4. 确保铸造和销毁操作符合 ERC20 标准。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，覆盖铸造和销毁场景。

## 验收标准
- [ ] 铸造和销毁函数在不同货币之间正确执行。
- [ ] 权限控制机制有效，防止未授权操作。
- [ ] ERC20 标准合规性测试通过。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。