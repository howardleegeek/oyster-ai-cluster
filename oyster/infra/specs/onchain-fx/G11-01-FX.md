---
task_id: G11-01-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["contracts/Stablecoin.sol", "contracts/FXRouter.sol"]
executor: glm
---
task_id: G11-01-FX  
project: onchain-fx  
priority: 1  
depends_on: []  
modifies: ["contracts/Stablecoin.sol", "contracts/FXRouter.sol"]  

## 目标  
在多币种稳定币系统中实现高效的流动性路由机制。

## 技术方案  
- 在 `FXRouter.sol` 中添加一个函数，通过智能合约自动选择最佳路径进行稳定币兑换。  
- 修改 `Stablecoin.sol` 以支持多路径兑换，并记录每条路径的交易数据。  
- 使用 Solidity 的 `SafeMath` 库确保计算的安全性。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，覆盖主要兑换路径。  

## 验收标准  
- [ ] 兑换路径选择算法在模拟环境中运行正确。  
- [ ] 交易数据记录完整且可追溯。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。