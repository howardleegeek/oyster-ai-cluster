---
task_id: G5-01-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/Stablecoin.sol", "tests/test_stablecoin.py"]
executor: glm
---
task_id: G5-01-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/Stablecoin.sol", "tests/test_stablecoin.py"]
---
## 目标
实现多货币稳定币之间的基础原子兑换功能。

## 技术方案
1. 在 `Stablecoin.sol` 中添加一个 `exchange` 函数，支持不同稳定币之间的兑换。
2. 使用固定汇率或预言机获取汇率。
3. 在 `tests/test_stablecoin.py` 中添加测试用例，验证兑换功能的正确性。
4. 确保兑换操作符合 ERC20 标准。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，覆盖主要兑换场景。

## 验收标准
- [ ] 兑换函数在不同稳定币之间正确执行。
- [ ] 汇率计算准确无误。
- [ ] ERC20 标准合规性测试通过。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。