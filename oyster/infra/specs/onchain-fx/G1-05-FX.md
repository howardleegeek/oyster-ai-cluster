---
task_id: G1-05-FX
project: onchain-fx
priority: 2
depends_on: []
modifies: ["contracts/FXRouter.sol", "tests/test_fxrouter.py", "frontend/src/components/Exchange.js"]
executor: glm
---
task_id: G1-05-FX  
project: onchain-fx  
priority: 2  
depends_on: []  
modifies: ["contracts/FXRouter.sol", "tests/test_fxrouter.py", "frontend/src/components/Exchange.js"]  

---  
## 目标  
实现用户界面的交换功能，允许用户在不同稳定币之间进行交换。

## 技术方案  
1. 在 `Exchange.js` 组件中实现用户输入交换金额和选择稳定币的功能。  
2. 调用 `FXRouter.sol` 中的交换函数，执行区块链上的交换操作。  
3. 在 `tests/test_fxrouter.py` 中编写交换功能的集成测试，验证前端和智能合约的交互。  
4. 使用 React 的状态管理和错误处理，确保用户操作的流畅性和安全性。

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保交换功能的正确性。  

## 验收标准  
- [ ] 用户界面中可以实现不同稳定币之间的交换。  
- [ ] 交换操作可以正确执行并更新用户余额。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。