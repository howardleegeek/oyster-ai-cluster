---
task_id: G3-01-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/Stablecoin.sol", "tests/test_stablecoin.py"]
executor: glm
---
task_id: G3-01-FX
project: onchain-fx
priority: 1
depends_on: []
modifies: ["src/contracts/Stablecoin.sol", "tests/test_stablecoin.py"]
---
## 目标
实现多货币稳定币之间的原子交换功能。

## 技术方案
1. 在 `Stablecoin.sol` 中添加一个 `atomicSwap` 函数，使用哈希时间锁（HTLC）机制。
2. 实现一个内部函数来验证哈希值和时间戳。
3. 在 `tests/test_stablecoin.py` 中添加测试用例，验证原子交换的成功和失败场景。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保原子交换的正确性。

## 验收标准
- [ ] 原子交换函数能够在不同稳定币之间成功执行。
- [ ] 哈希时间锁机制能够正确地处理超时和哈希验证。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。