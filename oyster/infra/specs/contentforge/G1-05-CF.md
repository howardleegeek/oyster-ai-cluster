---
task_id: G1-05-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["app/main.py", "app/schemas.py", "tests/test_schemas.py"]
executor: glm
---
task_id: G1-05-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["app/main.py", "app/schemas.py", "tests/test_schemas.py"]
---
## 目标
增强内容生成的输入参数，支持更多AI生成选项。

## 技术方案
1. 在 `app/schemas.py` 中扩展 `GenerateContentRequest` 模型，添加更多可选参数，如内容长度、语气等。
2. 更新 `/generate-content` 端点以处理新的输入参数。
3. 修改AI模型调用逻辑，根据用户输入调整生成内容。
4. 编写测试，验证新参数的传递和处理。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] 新参数被正确传递到AI模型。
- [ ] AI生成的内容符合用户指定的参数。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。