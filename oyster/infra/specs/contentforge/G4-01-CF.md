---
task_id: G4-01-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
executor: glm
---
task_id: G4-01-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["main.py", "tests/test_main.py"]
---
## 目标
实现基础的 FastAPI 路由以支持文本内容生成。

## 技术方案
- 在 `main.py` 中添加一个 `/generate-text` 路由，接受文本生成请求。
- 使用简单的模板或占位符生成文本内容。
- 在 `tests/test_main.py` 中添加测试用例，验证路由的响应状态和内容格式。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保路由功能正常。

## 验收标准
- [ ] `/generate-text` 路由返回 200 状态码。
- [ ] 返回的文本内容符合预期格式。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。