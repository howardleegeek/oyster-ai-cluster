---
task_id: G12-04-CF
project: contentforge
priority: 3
depends_on: []
modifies: ["app/schemas.py", "app/main.py", "tests/test_main.py"]
executor: glm
---
task_id: G12-04-CF  
project: contentforge  
priority: 3  
depends_on: []  
modifies: ["app/schemas.py", "app/main.py", "tests/test_main.py"]  
---  
## 目标  
添加用户反馈功能，提升内容生成的可控性。  

## 技术方案  
1. 在 `app/schemas.py` 中添加新的请求模型，包含用户对生成内容的反馈。  
2. 在 `app/main.py` 中实现反馈处理逻辑，更新 AI 模型的输入以提升后续生成质量。  
3. 在 `tests/test_main.py` 中编写测试，验证反馈处理的正确性。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写测试，确保反馈处理正确。  

## 验收标准  
- [ ] 用户可以提交对生成内容的反馈。  
- [ ] 反馈被正确处理并影响后续内容生成。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。