---
task_id: G12-02-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["app/routers/content.py", "app/models.py", "tests/test_content.py"]
executor: glm
---
task_id: G12-02-CF  
project: contentforge  
priority: 2  
depends_on: []  
modifies: ["app/routers/content.py", "app/models.py", "tests/test_content.py"]  
---  
## 目标  
优化内容生成的性能，确保快速响应。  

## 技术方案  
1. 在 `app/routers/content.py` 中实现异步处理，使用 FastAPI 的 `BackgroundTasks`。  
2. 更新 `app/models.py` 以包含新的模型字段，如生成时间。  
3. 在 `tests/test_content.py` 中编写性能测试，确保生成时间在可接受的范围内。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写性能测试，确保响应时间。  

## 验收标准  
- [ ] 内容生成请求在 2 秒内返回。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。