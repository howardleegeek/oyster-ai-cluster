---
task_id: G1-02-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["app/main.py", "app/schemas.py", "tests/test_schemas.py"]
executor: glm
---
task_id: G1-02-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["app/main.py", "app/schemas.py", "tests/test_schemas.py"]
---
## 目标
定义内容生成请求和响应的数据模型，并实现基本的输入验证。

## 技术方案
1. 在 `app/schemas.py` 中定义 `GenerateContentRequest` 和 `GenerateContentResponse` Pydantic模型。
2. 在 `/generate-content` 端点中使用这些模型进行请求和响应的验证。
3. 添加输入验证，确保关键词不为空且长度合理。
4. 编写测试，验证验证逻辑和模型序列化。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] 请求包含 `keywords` 字段，且符合验证规则。
- [ ] 响应符合 `GenerateContentResponse` 模型。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。