---
task_id: G3-03-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["contentforge/api/routes/posts.py", "contentforge/tests/test_posts.py"]
executor: glm
---
task_id: G3-03-CF  
project: contentforge  
priority: 1  
depends_on: []  
modifies: ["contentforge/api/routes/posts.py", "contentforge/tests/test_posts.py"]  

---
## 目标
实现帖子更新和删除功能，完善帖子管理。

## 技术方案
- 在 `posts.py` 中添加 `/update_post/{post_id}` 和 `/delete_post/{post_id}` 路由。
- 实现通过 `post_id` 更新和删除帖子内容的功能。
- 在 `test_posts.py` 中添加测试用例，验证帖子更新和删除功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保帖子更新和删除功能正常。

## 验收标准
- [ ] 可以通过 `/update_post/{post_id}` 路由更新帖子内容。
- [ ] 可以通过 `/delete_post/{post_id}` 路由删除帖子。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。