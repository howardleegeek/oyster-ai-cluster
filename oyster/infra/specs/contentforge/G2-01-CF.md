---
task_id: G2-01-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["app/main.py", "tests/test_main.py"]
executor: glm
---
task_id: G2-01-CF  
project: contentforge  
priority: 1  
depends_on: []  
modifies: ["app/main.py", "tests/test_main.py"]  
---  
## 目标  
实现基础的内容生成接口，支持文本生成功能。  

## 技术方案  
在 `app/main.py` 中添加一个 FastAPI 路由 `/generate-text`，接收用户输入的关键词并返回生成的文本。使用现有的 AI 模型进行文本生成，并添加基础的错误处理。编写相应的单元测试，确保接口返回预期的响应格式。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础的单元测试，确保接口功能正常。  

## 验收标准  
- [ ] `/generate-text` 路由能够接收关键词并返回生成的文本。  
- [ ] 单元测试覆盖主要功能路径，pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。