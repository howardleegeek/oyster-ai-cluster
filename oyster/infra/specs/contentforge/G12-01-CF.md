---
task_id: G12-01-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["app/main.py", "app/schemas.py", "tests/test_main.py"]
executor: glm
---
task_id: G12-01-CF  
project: contentforge  
priority: 1  
depends_on: []  
modifies: ["app/main.py", "app/schemas.py", "tests/test_main.py"]  
---  
## 目标  
实现基本的 AI 驱动的社交媒体内容生成功能。  

## 技术方案  
1. 在 `app/main.py` 中添加一个 API 端点 `/generate-content`，接收用户输入的主题和平台类型。  
2. 使用 FastAPI 的依赖注入功能，调用 AI 模型生成内容。  
3. 更新 `app/schemas.py` 以包含新的请求和响应模型。  
4. 在 `tests/test_main.py` 中编写基础测试，确保端点返回符合预期的响应。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保功能正常。  

## 验收标准  
- [ ] API 端点 `/generate-content` 存在并可访问。  
- [ ] 接收主题和平台类型参数并返回生成的社交媒体内容。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。