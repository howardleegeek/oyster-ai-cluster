---
task_id: G3-01-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["contentforge/api/routes/posts.py", "contentforge/tests/test_posts.py"]
executor: glm
---
task_id: G3-01-CF  
project: contentforge  
priority: 1  
depends_on: []  
modifies: ["contentforge/api/routes/posts.py", "contentforge/tests/test_posts.py"]  

---
## 目标
实现基本的帖子创建API，确保能够存储和检索帖子内容。

## 技术方案
- 在 `posts.py` 中添加一个 `/create_post` 路由，使用 FastAPI 的 `POST` 方法接收帖子数据。
- 使用 Pydantic 模型验证输入数据。
- 将帖子数据存储到内存中的列表中（后续可扩展为数据库）。
- 在 `test_posts.py` 中添加测试用例，验证帖子创建和检索功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保 API 功能正常。

## 验收标准
- [ ] 可以通过 POST 请求创建新帖子。
- [ ] 可以通过 GET 请求检索已创建的帖子。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。