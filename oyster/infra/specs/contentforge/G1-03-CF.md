---
task_id: G1-03-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["app/main.py", "app/utils.py", "tests/test_utils.py"]
executor: glm
---
task_id: G1-03-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["app/main.py", "app/utils.py", "tests/test_utils.py"]
---
## 目标
实现内容生成请求的异步处理，以提高性能和处理能力。

## 技术方案
1. 在 `app/utils.py` 中实现一个异步任务队列，使用 `asyncio` 或 `celery`。
2. 修改 `/generate-content` 端点，使其将生成任务提交到队列，并立即返回任务ID。
3. 实现一个轮询端点 `/check-status`，允许用户检查任务状态。
4. 编写测试，验证任务提交和状态检查功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] POST请求到 `/generate-content` 返回任务ID。
- [ ] `/check-status` 端点返回任务状态。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。