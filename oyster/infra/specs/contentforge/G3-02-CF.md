---
task_id: G3-02-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["contentforge/api/routes/users.py", "contentforge/tests/test_users.py"]
executor: glm
---
task_id: G3-02-CF  
project: contentforge  
priority: 2  
depends_on: []  
modifies: ["contentforge/api/routes/users.py", "contentforge/tests/test_users.py"]  

---
## 目标
实现用户注册和登录功能，确保用户身份验证。

## 技术方案
- 在 `users.py` 中添加 `/register` 和 `/login` 路由。
- 使用 JWT 进行用户身份验证，生成和验证 JWT token。
- 在内存中存储用户数据（后续可扩展为数据库）。
- 在 `test_users.py` 中添加测试用例，验证用户注册、登录和身份验证。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保用户认证功能正常。

## 验收标准
- [ ] 用户可以通过 `/register` 路由注册新账号。
- [ ] 用户可以通过 `/login` 路由登录并获取 JWT token。
- [ ] JWT token 可以用于身份验证。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。