---
task_id: G5-02-CF
project: contentforge
priority: 2
depends_on: ["G5-01-CF"]
modifies: ["app/routers/content.py", "app/models.py", "tests/test_content.py"]
executor: glm
---
task_id: G5-02-CF
project: contentforge
priority: 2
depends_on: ["G5-01-CF"]
modifies: ["app/routers/content.py", "app/models.py", "tests/test_content.py"]
---
## 目标
优化内容生成性能，确保在高并发情况下响应时间不超过 500 毫秒。

## 技术方案
1. 在 `app/routers/content.py` 中实现异步处理，使用 FastAPI 的 `BackgroundTasks`。
2. 在 `app/models.py` 中优化 `GeneratedContent` 模型的数据库查询，使用索引和批量处理。
3. 在 `tests/test_content.py` 中编写性能测试，使用 `pytest` 和 `asyncio` 来模拟高并发请求。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] 异步处理实现并通过测试
- [ ] 高并发情况下响应时间不超过 500 毫秒
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS