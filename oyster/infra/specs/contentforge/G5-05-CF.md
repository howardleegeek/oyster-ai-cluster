---
task_id: G5-05-CF
project: contentforge
priority: 2
depends_on: ["G5-02-CF", "G5-04-CF"]
modifies: ["app/main.py", "app/routers/content.py", "tests/test_main.py"]
executor: glm
---
task_id: G5-05-CF
project: contentforge
priority: 2
depends_on: ["G5-02-CF", "G5-04-CF"]
modifies: ["app/main.py", "app/routers/content.py", "tests/test_main.py"]
---
## 目标
实现内容生成的缓存机制，减少重复生成相同内容的需求，提高系统效率。

## 技术方案
1. 在 `app/main.py` 中实现缓存逻辑，使用内存缓存（如 `cachetools` 库）或 Redis。
2. 在 `app/routers/content.py` 中修改 API 端点 `/generate-content` 以使用缓存。
3. 在 `tests/test_main.py` 中编写测试用例，验证缓存的有效性和缓存命中率。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] 缓存机制实现并通过测试
- [ ] 重复生成相同内容时返回缓存结果
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS