---
task_id: G1-04-CF
project: contentforge
priority: 3
depends_on: []
modifies: ["app/main.py", "app/routes.py", "tests/test_routes.py"]
executor: glm
---
task_id: G1-04-CF
project: contentforge
priority: 3
depends_on: []
modifies: ["app/main.py", "app/routes.py", "tests/test_routes.py"]
---
## 目标
实现内容生成结果的历史记录查询功能。

## 技术方案
1. 在 `app/routes.py` 中创建一个新的GET端点 `/history`，允许用户查询之前生成的内容。
2. 在 `app/main.py` 中添加逻辑，将每次生成的内容保存到数据库中。
3. 实现分页功能，以便用户可以分批查看历史记录。
4. 编写测试，验证历史记录的存储和查询功能。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试。

## 验收标准
- [ ] GET请求到 `/history` 返回用户的历史生成内容。
- [ ] 历史记录支持分页查询。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。