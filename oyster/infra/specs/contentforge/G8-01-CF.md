---
task_id: G8-01-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
executor: glm
---
task_id: G8-01-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["main.py", "models.py", "tests/test_main.py"]
---
## 目标
集成一个基于GPT-3的自动内容生成模块。

## 技术方案
在 `main.py` 中添加一个新的API端点 `/generate-content`，该端点接收用户输入的关键词和主题，调用 `models.py` 中的 `GPT3ContentGenerator` 类生成内容。更新 `tests/test_main.py` 以包含对新的端点的测试。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] 新API端点 `/generate-content` 返回生成的文本内容
- [ ] 生成的文本内容与用户提供的关键词和主题相关
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS