---
task_id: G5-04-CF
project: contentforge
priority: 3
depends_on: ["G5-01-CF", "G5-03-CF"]
modifies: ["app/schemas.py", "tests/test_schemas.py"]
executor: glm
---
task_id: G5-04-CF
project: contentforge
priority: 3
depends_on: ["G5-01-CF", "G5-03-CF"]
modifies: ["app/schemas.py", "tests/test_schemas.py"]
---
## 目标
定义和实现内容生成的输入和输出数据模式，确保数据的一致性和完整性。

## 技术方案
1. 在 `app/schemas.py` 中定义新的 Pydantic 模型 `ContentGenerationRequest` 和 `ContentGenerationResponse`，分别用于请求和响应的数据验证。
2. 更新 API 端点 `/generate-content` 以使用新的请求和响应模型。
3. 在 `tests/test_schemas.py` 中编写测试用例，验证数据模式的正确性和验证逻辑。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] 数据模式定义正确并通过验证
- [ ] API 端点使用新的数据模式
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS