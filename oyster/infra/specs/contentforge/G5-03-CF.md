---
task_id: G5-03-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["app/utils.py", "tests/test_utils.py"]
executor: glm
---
task_id: G5-03-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["app/utils.py", "tests/test_utils.py"]
---
## 目标
实现内容生成的质量控制功能，确保生成的内容符合预设的质量标准。

## 技术方案
1. 在 `app/utils.py` 中添加一个函数 `validate_content`，使用自然语言处理库（如 NLTK 或 spaCy）来检查文本内容的长度、语法和语义。
2. 在生成内容的逻辑中调用 `validate_content` 函数进行质量检查。
3. 在 `tests/test_utils.py` 中编写测试用例，验证不同类型的内容是否通过或被拒绝。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] `validate_content` 函数正确识别符合和不符合质量标准的内容
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS