---
task_id: G9-04-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["auth.py", "api_endpoints.py"]
executor: glm
---
task_id: G9-04-CF
project: contentforge
priority: 1
depends_on: []
modifies: ["auth.py", "api_endpoints.py"]
---
## 目标
加强身份验证和授权机制，确保系统安全。

## 技术方案
- 在 `auth.py` 中集成 OAuth 2.0 协议，支持第三方身份验证提供商。
- 修改 `api_endpoints.py`，为所有 API 端点添加基于角色的访问控制（RBAC）。
- 添加日志记录，追踪身份验证和授权事件。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 写基础测试，确保身份验证和授权机制的正确性。

## 验收标准
- [ ] OAuth 2.0 集成正确，用户可以通过第三方身份验证提供商登录。
- [ ] RBAC 正确实施，不同角色的用户拥有不同的访问权限。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。