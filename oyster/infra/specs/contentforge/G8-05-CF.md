---
task_id: G8-05-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["main.py", "schemas.py", "tests/test_schemas.py"]
executor: glm
---
task_id: G8-05-CF
project: contentforge
priority: 2
depends_on: []
modifies: ["main.py", "schemas.py", "tests/test_schemas.py"]
---
## 目标
实现多语言支持以扩展内容生成的范围。

## 技术方案
在 `schemas.py` 中更新请求和响应模型以包含语言选择字段。在 `main.py` 中修改现有的内容生成API端点以支持多语言内容生成。更新 `tests/test_schemas.py` 以包含对新的语言选择字段的测试。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 写基础测试

## 验收标准
- [ ] 新增的语言选择字段在API请求中有效
- [ ] 生成的内容与选择的语言一致
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS