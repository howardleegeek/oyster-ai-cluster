---
task_id: G11-01-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/tokenization.py", "api/models/rwa_asset.py"]
executor: glm
---
task_id: G11-01-GP  
project: gem-platform  
priority: 1  
depends_on: []  
modifies: ["api/routes/tokenization.py", "api/models/rwa_asset.py"]  

---
## 目标
实现基于 FastAPI 的 RWA（现实世界资产）资产代币化 API，支持 IP、诉讼和奢侈品资产。

## 技术方案
- 在 `api/routes/tokenization.py` 中添加新的路由 `/tokenize`，处理不同类型资产的代币化请求。
- 在 `api/models/rwa_asset.py` 中扩展 `RWAsset` 模型，包含资产类型、所有权信息和元数据。
- 使用 Python 的 `dataclasses` 模块来简化模型定义，并确保数据验证。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，覆盖代币化流程的主要路径。

## 验收标准
- [ ] 新增的 `/tokenize` 路由能够正确处理 IP、诉讼和奢侈品资产的代币化请求。
- [ ] `RWAsset` 模型能够正确存储和验证不同类型资产的数据。
- [ ] 基础测试覆盖代币化流程的主要路径。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。