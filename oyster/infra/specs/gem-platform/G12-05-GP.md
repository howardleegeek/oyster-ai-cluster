---
task_id: G12-05-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/routes/legal.py", "api/models/legal.py"]
executor: glm
---
task_id: G12-05-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/routes/legal.py", "api/models/legal.py"]
---
## 目标
实现基于智能合约的法律诉讼资产代币化功能。

## 技术方案
1. 在 `legal.py` 中添加一个 FastAPI 路由 `/tokenize-legal-asset`，用于接收法律诉讼资产信息和调用相应的智能合约函数。
2. 在 `legal.py` 中扩展 `LegalAsset` 模型以包含法律诉讼资产特有的属性，如案件类型、描述和诉讼状态。
3. 使用 Solidity 编写一个基础的法律诉讼资产代币化智能合约，并将其与 FastAPI 后端集成。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保法律诉讼资产代币化功能正常工作。

## 验收标准
- [ ] 代币化路由 `/tokenize-legal-asset` 正常接收请求并返回代币 ID。
- [ ] 代币化后的法律诉讼资产信息正确存储在数据库中。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。