---
task_id: G11-02-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/services/nft_service.py", "api/routes/nft.py"]
executor: glm
---
task_id: G11-02-GP  
project: gem-platform  
priority: 2  
depends_on: []  
modifies: ["api/services/nft_service.py", "api/routes/nft.py"]  

---
## 目标
实现 NFT 交易的基础功能，包括创建和转移 NFT。

## 技术方案
- 在 `api/services/nft_service.py` 中实现 `create_nft` 和 `transfer_nft` 方法，处理 NFT 的创建和转移逻辑。
- 在 `api/routes/nft.py` 中添加新的路由 `/create` 和 `/transfer`，分别处理 NFT 的创建和转移请求。
- 使用 FastAPI 的依赖注入机制，确保 NFT 服务的高内聚和低耦合。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，覆盖 NFT 创建和转移的主要路径。

## 验收标准
- [ ] 新增的 `/create` 路由能够正确创建 NFT。
- [ ] 新增的 `/transfer` 路由能够正确转移 NFT。
- [ ] 基础测试覆盖 NFT 创建和转移的主要路径。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。