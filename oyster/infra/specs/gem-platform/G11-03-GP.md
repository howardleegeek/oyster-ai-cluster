---
task_id: G11-03-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/tokenization.py", "api/services/tokenization_service.py"]
executor: glm
---
task_id: G11-03-GP  
project: gem-platform  
priority: 1  
depends_on: []  
modifies: ["api/routes/tokenization.py", "api/services/tokenization_service.py"]  

---
## 目标
增强 RWA 资产代币化的安全性和数据验证。

## 技术方案
- 在 `api/services/tokenization_service.py` 中实现数据验证逻辑，确保代币化请求的完整性和合法性。
- 在 `api/routes/tokenization.py` 中调用新的数据验证逻辑，并在必要时返回详细的错误信息。
- 使用 Python 的 `pydantic` 库进行数据验证，确保所有必要字段都存在且格式正确。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，覆盖数据验证的主要路径。

## 验收标准
- [ ] 代币化请求在缺少必要字段时返回 400 错误。
- [ ] 代币化请求在字段格式错误时返回 400 错误。
- [ ] 基础测试覆盖数据验证的主要路径。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。