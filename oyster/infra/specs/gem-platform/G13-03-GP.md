---
task_id: G13-03-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/ip.py", "api/models/ip_asset.py"]
executor: glm
---
task_id: G13-03-GP  
project: gem-platform  
priority: 1  
depends_on: []  
modifies: ["api/routes/ip.py", "api/models/ip_asset.py"]  
---  
## 目标  
实现 IP 资产的代币化 API 接口。  

## 技术方案  
- 在 `ip.py` 中添加 `/tokenize_ip` 端点，处理 IP 资产的上传和代币化请求。  
- 在 `models/ip_asset.py` 中定义 IP 资产数据模型，包含资产名称、描述、所有者信息、代币 ID 等字段。  
- 实现 IP 资产数据的验证和存储逻辑。  

## 约束  
- 在现有代码库内修改，不新建超过 3 个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，验证 IP 资产代币化流程的正确性。  

## 验收标准  
- [ ] IP 资产代币化 API 端点可用，返回正确的代币信息。  
- [ ] 基础测试覆盖主要功能路径，pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。