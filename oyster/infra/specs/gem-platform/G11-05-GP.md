---
task_id: G11-05-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/models/rwa_asset.py", "api/services/tokenization_service.py"]
executor: glm
---
task_id: G11-05-GP  
project: gem-platform  
priority: 2  
depends_on: []  
modifies: ["api/models/rwa_asset.py", "api/services/tokenization_service.py"]  

---
## 目标
实现 RWA 资产的分类和标签功能，方便用户搜索和筛选。

## 技术方案
- 在 `api/models/rwa_asset.py` 中扩展 `RWAsset` 模型，添加 `category` 和 `tags` 字段。
- 在 `api/services/tokenization_service.py` 中实现分类和标签的验证逻辑，确保分类和标签的合法性。
- 在 FastAPI 的路由中实现基于分类和标签的过滤功能，使用 SQLAlchemy 的查询功能进行数据筛选。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，覆盖分类和标签功能的主要路径。

## 验收标准
- [ ] 新增的分类和标签字段能够正确存储和验证。
- [ ] 基于分类和标签的过滤功能能够正确返回结果。
- [ ] 基础测试覆盖分类和标签功能的主要路径。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。