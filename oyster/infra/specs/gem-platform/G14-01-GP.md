---
task_id: G14-01-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/tokenization.py", "api/models/asset.py"]
executor: glm
---
task_id: G14-01-GP  
project: gem-platform  
priority: 1  
depends_on: []  
modifies: ["api/routes/tokenization.py", "api/models/asset.py"]  

---

## 目标
实现对奢侈品资产的代币化功能，支持ERC-721标准。

## 技术方案
1. 在 `api/routes/tokenization.py` 中添加新的API端点 `/luxury-assets/tokenize`，用于接收奢侈品资产数据并生成对应的NFT。
2. 在 `api/models/asset.py` 中扩展 `Asset` 模型，添加奢侈品资产特有的字段，如品牌、型号和年份。
3. 使用Python FastAPI处理请求并调用Solidity智能合约生成NFT。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保API端点正确生成NFT。

## 验收标准
- [ ] API端点 `/luxury-assets/tokenize` 正确处理请求并返回NFT地址。
- [ ] 生成的NFT符合ERC-721标准。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。