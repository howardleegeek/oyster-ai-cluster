---
task_id: G10-03-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/endpoints.py", "api/schemas.py"]
executor: glm
---
task_id: G10-03-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/endpoints.py", "api/schemas.py"]
---
## 目标
实现IP资产代币化的API端点。

## 技术方案
1. 在 `api/schemas.py` 中定义IP资产的数据模型。
2. 在 `api/endpoints.py` 中创建CRUD端点来处理IP资产的创建、读取、更新和删除。
3. 确保所有端点都经过RBAC中间件的保护。

## 约束
- 在现有代码库内修改，不新建超过3个文件
- 不留 TODO/FIXME/placeholder
- 编写针对IP资产端点的单元测试

## 验收标准
- [ ] 可以通过API创建、读取、更新和删除IP资产
- [ ] RBAC正确地限制了IP资产的访问
- [ ] pytest 全绿

## 不要做
- 不重构现有代码
- 不改 UI/CSS