---
task_id: G9-05-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/services/asset_service.py", "api/models/asset.py", "tests/test_asset.py"]
executor: glm
---
task_id: G9-05-GP  
project: gem-platform  
priority: 2  
depends_on: []  
modifies: ["api/services/asset_service.py", "api/models/asset.py", "tests/test_asset.py"]  
---  
## 目标  
实现奢侈品资产的代币化功能。  

## 技术方案  
- 在 `api/models/asset.py` 中扩展 `Asset` 模型，添加奢侈品资产特有的字段，如 `brand` 和 `condition`。  
- 在 `api/services/asset_service.py` 中实现奢侈品资产的代币化逻辑。  
- 在 `tests/test_asset.py` 中编写测试用例，验证奢侈品资产代币化的正确性和异常处理。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保代币化过程的正确性。  

## 验收标准  
- [ ] 奢侈品资产代币化功能正确实现。  
- [ ] `Asset` 模型包含奢侈品资产特有的字段。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。