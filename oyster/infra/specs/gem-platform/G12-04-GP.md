---
task_id: G12-04-GP
project: gem-platform
priority: 3
depends_on: []
modifies: ["api/routes/auction.py", "api/models/auction.py"]
executor: glm
---
task_id: G12-04-GP
project: gem-platform
priority: 3
depends_on: []
modifies: ["api/routes/auction.py", "api/models/auction.py"]
---
## 目标
实现基于智能合约的 NFT 拍卖功能。

## 技术方案
1. 在 `auction.py` 中添加一个 FastAPI 路由 `/create-nft-auction`，用于接收拍卖信息和调用相应的智能合约函数。
2. 在 `auction.py` 中扩展 `Auction` 模型以包含拍卖特有的属性，如起拍价、截止时间和当前出价。
3. 使用 Solidity 编写一个基础的 NFT 拍卖智能合约，并将其与 FastAPI 后端集成。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保 NFT 拍卖功能正常工作。

## 验收标准
- [ ] 拍卖创建路由 `/create-nft-auction` 正常接收请求并返回拍卖 ID。
- [ ] 拍卖信息正确存储在数据库中。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。