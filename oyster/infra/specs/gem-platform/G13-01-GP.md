---
task_id: G13-01-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/asset_tokenization.py", "api/models/token.py"]
executor: glm
---
task_id: G13-01-GP  
project: gem-platform  
priority: 1  
depends_on: []  
modifies: ["api/routes/asset_tokenization.py", "api/models/token.py"]  
---  
## 目标  
实现 RWA 资产的代币化 API 接口。  

## 技术方案  
- 在 `asset_tokenization.py` 中添加 `/tokenize` 端点，处理资产上传和代币化请求。  
- 在 `token.py` 中定义新的数据模型，包含资产类型、所有者信息、代币 ID 等字段。  
- 使用 FastAPI 的依赖注入机制，确保数据验证和安全性。  

## 约束  
- 在现有代码库内修改，不新建超过 3 个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，验证代币化流程的正确性。  

## 验收标准  
- [ ] 代币化 API 端点可用，返回正确的代币信息。  
- [ ] 基础测试覆盖主要功能路径，pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。