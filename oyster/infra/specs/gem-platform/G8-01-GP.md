---
task_id: G8-01-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/assets.py", "api/models/asset.py", "tests/test_assets.py"]
executor: glm
---
task_id: G8-01-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/assets.py", "api/models/asset.py", "tests/test_assets.py"]
---
## 目标
实现基于AI的资产价值预测功能，用于RWA代币化平台。

## 技术方案
1. 集成预训练的AI模型（如TensorFlow或PyTorch）到FastAPI后端。
2. 创建新的API端点`/api/assets/predict`来接收资产数据并返回预测价值。
3. 在`asset`模型中添加新字段`predicted_value`以存储预测结果。
4. 编写相应的序列化器和数据验证逻辑。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保预测端点的正确性。

## 验收标准
- [ ] 新增的API端点`/api/assets/predict`返回有效的预测价值。
- [ ] 在资产模型中正确存储预测价值。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。