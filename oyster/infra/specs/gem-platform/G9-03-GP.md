---
task_id: G9-03-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/monitoring/monitoring.py", "api/endpoints/status.py", "tests/test_monitoring.py"]
executor: glm
---
task_id: G9-03-GP  
project: gem-platform  
priority: 1  
depends_on: []  
modifies: ["api/monitoring/monitoring.py", "api/endpoints/status.py", "tests/test_monitoring.py"]  
---  
## 目标  
增强系统监控功能，实时跟踪关键指标。  

## 技术方案  
- 在 `api/monitoring/monitoring.py` 中集成 Prometheus，用于收集和存储系统指标。  
- 在 `api/endpoints/status.py` 中添加新的端点 `/status/metrics`，用于暴露 Prometheus 指标。  
- 在 `tests/test_monitoring.py` 中编写测试用例，验证指标的正确暴露和收集。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保监控功能的可靠性。  

## 验收标准  
- [ ] Prometheus 成功收集并存储系统指标。  
- [ ] `/status/metrics` 端点正确暴露 Prometheus 指标。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。