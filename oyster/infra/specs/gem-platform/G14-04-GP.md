---
task_id: G14-04-GP
project: gem-platform
priority: 3
depends_on: []
modifies: ["api/routes/analytics.py", "api/models/analytics.py"]
executor: glm
---
task_id: G14-04-GP  
project: gem-platform  
priority: 3  
depends_on: []  
modifies: ["api/routes/analytics.py", "api/models/analytics.py"]  

---

## 目标
实现平台数据分析功能，提供用户行为和交易数据的分析。

## 技术方案
1. 在 `api/routes/analytics.py` 中添加新的API端点 `/analytics/user-behavior` 和 `/analytics/transactions`，用于获取用户行为和交易数据。
2. 在 `api/models/analytics.py` 中定义数据分析模型，包括用户行为和交易数据的结构。
3. 使用Python FastAPI处理请求并从数据库中提取数据。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保数据分析功能正常。

## 验收标准
- [ ] API端点 `/analytics/user-behavior` 和 `/analytics/transactions` 返回正确的数据结构。
- [ ] 数据分析模型正确反映用户行为和交易数据。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。