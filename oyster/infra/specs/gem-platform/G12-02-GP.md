---
task_id: G12-02-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/routes/nft.py", "api/models/nft.py"]
executor: glm
---
task_id: G12-02-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/routes/nft.py", "api/models/nft.py"]
---
## 目标
增强 NFT 交易功能，支持批量上传和交易。

## 技术方案
1. 在 `nft.py` 中添加一个 FastAPI 路由 `/upload-nft-batch`，用于接收批量 NFT 数据并调用相应的智能合约函数。
2. 在 `nft.py` 中扩展 `NFT` 模型以支持批量操作，如批量上传和批量交易。
3. 更新智能合约以支持批量 NFT 交易，并将其与 FastAPI 后端集成。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保批量上传和交易功能正常工作。

## 验收标准
- [ ] 批量上传路由 `/upload-nft-batch` 正常接收请求并返回上传结果。
- [ ] 批量交易功能在智能合约中正确执行。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。