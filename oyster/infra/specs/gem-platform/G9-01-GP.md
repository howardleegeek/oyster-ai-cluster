---
task_id: G9-01-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/endpoints/tokenization.py", "api/models/asset.py", "tests/test_tokenization.py"]
executor: glm
---
task_id: G9-01-GP  
project: gem-platform  
priority: 1  
depends_on: []  
modifies: ["api/endpoints/tokenization.py", "api/models/asset.py", "tests/test_tokenization.py"]  
---  
## 目标  
实现对 IP 资产的代币化 API 支持。  

## 技术方案  
- 在 `api/endpoints/tokenization.py` 中添加新的 API 端点 `/tokenize/ip`，用于接收 IP 资产数据并调用代币化逻辑。  
- 在 `api/models/asset.py` 中扩展 `Asset` 模型，添加 IP 资产特有的字段，如 `license_type` 和 `expiration_date`。  
- 在 `tests/test_tokenization.py` 中编写测试用例，验证 IP 资产代币化的正确性和异常处理。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保代币化过程的正确性。  

## 验收标准  
- [ ] API 端点 `/tokenize/ip` 正确接收 IP 资产数据并返回代币化结果。  
- [ ] `Asset` 模型包含 IP 资产特有的字段。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。