---
task_id: G12-03-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/ip.py", "api/models/ip.py"]
executor: glm
---
task_id: G12-03-GP
project: gem-platform
priority: 1
depends_on: []
modifies: ["api/routes/ip.py", "api/models/ip.py"]
---
## 目标
实现基于智能合约的 IP 资产代币化功能。

## 技术方案
1. 在 `ip.py` 中添加一个 FastAPI 路由 `/tokenize-ip-asset`，用于接收 IP 资产信息和调用相应的智能合约函数。
2. 在 `ip.py` 中扩展 `IPAsset` 模型以包含 IP 资产特有的属性，如资产类型、描述和权利范围。
3. 使用 Solidity 编写一个基础的 IP 资产代币化智能合约，并将其与 FastAPI 后端集成。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保 IP 资产代币化功能正常工作。

## 验收标准
- [ ] 代币化路由 `/tokenize-ip-asset` 正常接收请求并返回代币 ID。
- [ ] 代币化后的 IP 资产信息正确存储在数据库中。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。