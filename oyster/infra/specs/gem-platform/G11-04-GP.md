---
task_id: G11-04-GP
project: gem-platform
priority: 3
depends_on: []
modifies: ["api/routes/nft.py", "api/services/nft_service.py"]
executor: glm
---
task_id: G11-04-GP  
project: gem-platform  
priority: 3  
depends_on: []  
modifies: ["api/routes/nft.py", "api/services/nft_service.py"]  

---
## 目标
实现 NFT 拍卖功能，允许用户对 NFT 进行竞价。

## 技术方案
- 在 `api/services/nft_service.py` 中实现 `start_auction` 和 `place_bid` 方法，处理 NFT 拍卖的启动和竞价逻辑。
- 在 `api/routes/nft.py` 中添加新的路由 `/start_auction` 和 `/place_bid`，分别处理拍卖启动和竞价请求。
- 使用数据库事务，确保拍卖和竞价操作的原子性。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，覆盖拍卖和竞价的主要路径。

## 验收标准
- [ ] 新增的 `/start_auction` 路由能够正确启动 NFT 拍卖。
- [ ] 新增的 `/place_bid` 路由能够正确处理竞价请求。
- [ ] 基础测试覆盖拍卖和竞价的主要路径。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。