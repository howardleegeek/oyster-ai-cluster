---
task_id: G14-05-GP
project: gem-platform
priority: 2
depends_on: []
modifies: ["api/routes/nft.py", "api/models/nft.py"]
executor: glm
---
task_id: G14-05-GP  
project: gem-platform  
priority: 2  
depends_on: []  
modifies: ["api/routes/nft.py", "api/models/nft.py"]  

---

## 目标
实现NFT的批量操作功能，支持批量购买和批量转移。

## 技术方案
1. 在 `api/routes/nft.py` 中添加新的API端点 `/nft/batch-buy` 和 `/nft/batch-transfer`，用于处理NFT的批量购买和转移请求。
2. 在 `api/models/nft.py` 中扩展 `NFT` 模型，添加批量操作的字段，如购买数量和转移列表。
3. 使用Python FastAPI处理请求并调用智能合约执行批量操作。

## 约束
- 在现有代码库内修改，不新建超过3个文件。
- 不留 TODO/FIXME/placeholder。
- 编写基础测试，确保批量操作功能正常。

## 验收标准
- [ ] API端点 `/nft/batch-buy` 和 `/nft/batch-transfer` 正确处理请求并更新NFT状态。
- [ ] 智能合约方法正确执行批量操作逻辑。
- [ ] pytest 全绿。

## 不要做
- 不重构现有代码。
- 不改 UI/CSS。