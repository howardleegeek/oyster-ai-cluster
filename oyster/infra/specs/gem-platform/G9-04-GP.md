---
task_id: G9-04-GP
project: gem-platform
priority: 3
depends_on: []
modifies: ["api/security/auth.py", "api/endpoints/login.py", "tests/test_auth.py"]
executor: glm
---
task_id: G9-04-GP  
project: gem-platform  
priority: 3  
depends_on: []  
modifies: ["api/security/auth.py", "api/endpoints/login.py", "tests/test_auth.py"]  
---  
## 目标  
加强用户认证机制，引入多因素认证 (MFA)。  

## 技术方案  
- 在 `api/security/auth.py` 中实现 MFA 逻辑，生成和验证一次性密码 (OTP)。  
- 在 `api/endpoints/login.py` 中修改登录流程，添加 MFA 验证步骤。  
- 在 `tests/test_auth.py` 中编写测试用例，验证 MFA 的正确性和安全性。  

## 约束  
- 在现有代码库内修改，不新建超过3个文件。  
- 不留 TODO/FIXME/placeholder。  
- 编写基础测试，确保 MFA 功能的可靠性。  

## 验收标准  
- [ ] 用户在登录时能够成功完成 MFA 验证。  
- [ ] MFA 逻辑正确生成和验证 OTP。  
- [ ] pytest 全绿。  

## 不要做  
- 不重构现有代码。  
- 不改 UI/CSS。