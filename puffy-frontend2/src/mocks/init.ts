// import { setupWorker } from 'msw/browser';
// import { getRewardCenterApiMock } from '../types/api/reward-center-api/reward-center-api.msw';
// 
// if (process.env.NODE_ENV === 'development' && typeof window !== 'undefined') {
//   // const worker = setupWorker(...getRewardCenterApiMock());
//   // worker.start({
//   //   onUnhandledRequest: 'bypass',
//   // });
// }